---
phase: 04-deterministic-run-state-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/lib/commands.ts
  - src/features/board/column-inference.ts
  - src/features/board/column-inference.test.ts
  - src/features/board/hooks/useBoardInteractions.ts
  - src/features/board/hooks/useBoardInteractions.test.ts
  - src/features/board/components/KanbanBoard.tsx
  - src/features/board/components/IssueDetailsPanel.tsx
  - src/intake/policy-reasons.ts
  - src/intake/policy-reasons.test.ts
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
must_haves:
  truths:
    - "Cards and issue details panel show canonical runtime stage names (`queued`, `preparing`, `coding`, `validating`, `publishing`) for active runs."
    - "Queued runs show explicit queue position, and non-terminal runtime stages stay mapped to `In Progress`."
    - "Terminal outcomes map columns as `success -> In Review`, `failed|cancelled|guardrail_blocked -> Todo`, with durable reasonCode/fixHint metadata visible for inspection."
    - "History rendering is newest-first and limited to the latest 20 runs per issue from persisted backend data."
  artifacts:
    - path: src/lib/commands.ts
      provides: "Typed runtime snapshot/history command wrappers and payload contracts."
      contains: "runtimeGetRepositoryRunSnapshot"
    - path: src/features/board/hooks/useBoardInteractions.ts
      provides: "Runtime metadata hydration plus event-stream merge for board/detail state."
      contains: "listen(\"runtime/run-stage-changed\""
    - path: src/features/board/components/KanbanBoard.tsx
      provides: "Card-level runtime stage and queue-position metadata rendering."
      contains: "queue position"
    - path: src/features/board/components/IssueDetailsPanel.tsx
      provides: "Issue-level terminal status and transition timeline rendering."
      contains: "Runtime history"
  key_links:
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/lib/commands.ts
      via: "Hook fetches runtime snapshot/history via typed invoke wrappers"
      pattern: "runtimeGetRepositoryRunSnapshot"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/features/board/column-inference.ts
      via: "Runtime stage overlay controls deterministic column mapping"
      pattern: "inferDefaultColumn"
    - from: src/intake/policy-reasons.ts
      to: src/features/board/components/IssueDetailsPanel.tsx
      via: "Recovery failures expose reasonCode/fixHint semantics in user-visible messaging"
      pattern: "runtime_recovery_process_lost"
---

<objective>
Deliver deterministic runtime stage and history visibility in the board UI and issue details panel.

Purpose: Surface canonical runtime lifecycle and recovered terminal outcomes without changing locked drag/intake semantics.
Output: Frontend snapshot/event integration, runtime-aware column mapping, and stage/history UI rendering.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deterministic-run-state-engine/04-CONTEXT.md
@.planning/phases/04-deterministic-run-state-engine/04-RESEARCH.md
@.planning/phases/04-deterministic-run-state-engine/04-02-PLAN.md
@src/lib/commands.ts
@src/features/board/column-inference.ts
@src/features/board/hooks/useBoardInteractions.ts
@src/features/board/components/KanbanBoard.tsx
@src/features/board/components/IssueDetailsPanel.tsx
@src/intake/policy-reasons.ts

<interfaces>
From `src/features/board/column-inference.ts`:
```typescript
export const inferDefaultColumn = (item: ColumnInferenceItem): KanbanColumnKey => { ... };
```

From `src/features/board/hooks/useBoardInteractions.ts`:
```typescript
const groupedItemsByColumn = createMemo(() => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed runtime snapshot/history contracts and hook hydration + event sync</name>
  <files>src/lib/commands.ts, src/features/board/hooks/useBoardInteractions.ts, src/features/board/hooks/useBoardInteractions.test.ts</files>
  <behavior>
    - Hook fetches runtime snapshot for selected repository and merges metadata by issue number.
    - Hook subscribes to `runtime/run-stage-changed` events and applies deltas until cleanup.
    - Runtime metadata survives background GitHub refresh without regressing existing drag/intake flows.
  </behavior>
  <action>Add frontend command interfaces/wrappers for runtime snapshot and issue history payloads, then extend `useBoardInteractions` with runtime metadata state, startup hydration, and event listener lifecycle management. Expand hook tests to verify snapshot merge, event delta application, and cleanup/unlisten behavior.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/hooks/useBoardInteractions.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Renderer has a deterministic runtime metadata data source that combines snapshot truth with live stage updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply runtime-aware deterministic column mapping rules with tests</name>
  <files>src/features/board/column-inference.ts, src/features/board/column-inference.test.ts, src/features/board/hooks/useBoardInteractions.ts</files>
  <behavior>
    - Runtime non-terminal stages (`queued`, `preparing`, `coding`, `validating`, `publishing`) map to `inProgress`.
    - Runtime terminal statuses map `success -> inReview` and `failed|cancelled|guardrail_blocked -> todo`.
    - Existing GitHub-label inference remains fallback when runtime metadata is absent.
  </behavior>
  <action>Extend board column inference contracts to optionally accept runtime stage/terminal metadata and apply locked precedence rules from CONTEXT.md. Update grouping logic in `useBoardInteractions` to pass runtime metadata through inference and keep current optimistic drag semantics unchanged. Add table-driven inference tests for runtime overlay precedence.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/column-inference.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Column placement reflects deterministic runtime state rules while preserving existing fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render stage badges, queue position, terminal metadata, and newest-first history in board/details surfaces</name>
  <files>src/features/board/components/KanbanBoard.tsx, src/features/board/components/IssueDetailsPanel.tsx, src/intake/policy-reasons.ts, src/intake/policy-reasons.test.ts</files>
  <behavior>
    - Kanban cards display canonical stage text and queue-position badge for queued runs.
    - Issue details panel shows current stage, terminal status metadata, and newest-first run history list.
    - Recovery process-loss reason codes resolve through policy reason copy with `reasonCode` + `fixHint` semantics.
  </behavior>
  <action>Render runtime metadata in board/details surfaces through existing board interaction data flow (avoid introducing new top-level layout wiring), then add stage/queue/terminal/history UI using existing Tailwind/token patterns (no hardcoded colors). Extend `policy-reasons.ts` with recovery-specific reason code copy and add tests for mapped/fallback behavior.</action>
  <verify>
    <automated>pnpm exec vitest run src/intake/policy-reasons.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>User can inspect deterministic run stage progression and durable terminal outcomes from both card metadata and issue details panel.</done>
</task>

</tasks>

<verification>
- Snapshot + event integration drives runtime metadata state in board interactions.
- Runtime-aware inference enforces locked column mapping semantics.
- Card/details UI surfaces show stage, queue position, terminal metadata, and newest-first history with structured reasonCode/fixHint semantics.
</verification>

<success_criteria>
- ORCH-01 is satisfied by visible deterministic stage progression and terminal outcome inspection surfaces.
- ORCH-02 is satisfied at UI boundary by restart-safe snapshot hydration and recovered-state visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-run-state-engine/04-03-SUMMARY.md`.
</output>
