---
phase: 04-deterministic-run-state-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src-tauri/src/runtime_boundary.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/commands.rs
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
must_haves:
  truths:
    - "App startup reconciles persisted non-terminal run state immediately before user interaction."
    - "Unrecoverable in-flight runs are finalized as `failed` with explicit recovery reason metadata and fix hints."
    - "Backend exposes runtime snapshot/history command surfaces and emits stage-change events for deterministic UI hydration."
  artifacts:
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Startup reconciliation, persisted snapshot projection, and runtime stage event payload emission."
      contains: "fn reconcile_runtime_state_on_startup"
    - path: src-tauri/src/lib.rs
      provides: "App setup wiring that invokes reconciliation during startup."
      contains: "reconcile_runtime_state_on_startup"
    - path: src-tauri/src/commands.rs
      provides: "Read commands for runtime snapshot and issue history payloads."
      contains: "runtime_get_repository_run_snapshot"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "Tauri setup invokes reconciliation before steady-state UI operations"
      pattern: "setup"
    - from: src-tauri/src/commands.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "Runtime read commands fetch persisted stage and history projections"
      pattern: "runtime_get_repository_run_snapshot"
    - from: src-tauri/src/runtime_boundary.rs
      to: src/features/board/hooks/useBoardInteractions.ts
      via: "Rust emits runtime stage-changed event consumed by board runtime metadata hook"
      pattern: "runtime/run-stage-changed"
---

<objective>
Add deterministic recovery and read surfaces on top of persisted runtime state.

Purpose: Ensure restart/crash reconciliation is automatic and deterministic, and provide the backend contracts UI needs to render canonical stage/history state.
Output: Startup reconciliation, read commands, and event emission for runtime stage/state updates.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deterministic-run-state-engine/04-CONTEXT.md
@.planning/phases/04-deterministic-run-state-engine/04-RESEARCH.md
@.planning/phases/04-deterministic-run-state-engine/04-01-PLAN.md
@src-tauri/src/lib.rs
@src-tauri/src/commands.rs
@src-tauri/src/runtime_boundary.rs

<interfaces>
From `src-tauri/src/lib.rs`:
```rust
tauri::Builder::default()
    .setup(|app| { ... })
    .invoke_handler(tauri::generate_handler![ ... ]);
```

From `src-tauri/src/runtime_boundary.rs`:
```rust
#[tauri::command]
pub async fn runtime_enqueue_issue_run(...) -> Result<RuntimeQueueOutcome, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile persisted runtime state at startup with deterministic failure finalization</name>
  <files>src-tauri/src/runtime_boundary.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Reconciliation executes during startup before runtime metadata is consumed by UI.
    - Persisted queued runs are restored deterministically with FIFO ordering.
    - Persisted in-flight runs that cannot be reattached become `failed` with dedicated recovery reason metadata.
  </behavior>
  <action>Implement a startup reconciliation entrypoint in `runtime_boundary.rs` that loads persisted non-terminal rows, restores queue state for recoverable queued runs, finalizes unrecoverable in-flight states as failed with dedicated recovery reason/fix hint metadata, and resumes deterministic queue progression. Wire this function into `lib.rs` setup after DB initialization and before normal runtime operations continue.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Restart/crash paths reconcile deterministically with no orphaned or duplicated active runs per repository.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add runtime snapshot and issue-history read commands</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/runtime_boundary.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Snapshot payload exposes per-issue stage, queue position, and terminal metadata for selected repository.
    - Issue history payload is newest-first and bounded to the last 20 persisted runs.
    - Payloads preserve `reasonCode` and `fixHint` semantics for terminal/recovery outcomes.
  </behavior>
  <action>Add typed read/query helpers in `runtime_boundary.rs`, expose them through new `#[tauri::command]` handlers in `commands.rs`, and register these handlers in `lib.rs`. Keep serialization camelCase-compatible with frontend wrappers and ensure queries read from persisted tables introduced in Plan 04-01.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo check</automated>
  </verify>
  <done>Frontend can request deterministic runtime snapshots and bounded issue history directly from persisted state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit stage-change events from authoritative runtime transition paths</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Event payload includes repository identity, issue number, current stage, queue position, and terminal metadata when applicable.
    - Events are emitted for enqueue/transition/finalize/reconciliation updates from the authoritative write path.
    - Event emission failures do not corrupt runtime queue progression or persistence.
  </behavior>
  <action>Integrate `tauri::Emitter` calls into transition/finalization/reconciliation paths so every authoritative state mutation can notify the renderer. Reuse one payload model that aligns with snapshot command fields and includes recovery reason metadata where applicable.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Runtime state updates can be consumed as live deltas while remaining consistent with persisted snapshot truth.</done>
</task>

</tasks>

<verification>
- Startup always runs runtime reconciliation and finalizes unrecoverable in-flight runs with explicit recovery reason metadata.
- New runtime read commands compile and return deterministic snapshot/history payloads from persisted data.
- Stage-change event emission is wired into authoritative transition paths.
</verification>

<success_criteria>
- ORCH-02 recovery behavior is deterministic at app startup.
- ORCH-01 backend visibility contract exists for canonical stage and terminal metadata delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-run-state-engine/04-02-SUMMARY.md`.
</output>
