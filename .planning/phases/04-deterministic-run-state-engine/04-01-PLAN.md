---
phase: 04-deterministic-run-state-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/db.rs
  - src-tauri/src/runtime_boundary.rs
autonomous: true
requirements:
  - ORCH-02
must_haves:
  truths:
    - "Run stage state persists in SQLite so active and queued runs survive app restarts."
    - "Every stage change appends a timestamped transition entry tied to one canonical run record."
    - "Terminal metadata (`success|failed|cancelled|guardrail_blocked`, reasonCode, fixHint) is durable and retained as newest-first history capped at 20 runs per issue."
  artifacts:
    - path: src-tauri/src/db.rs
      provides: "Runtime run-state schema for canonical runs and transition timeline rows."
      contains: "CREATE TABLE IF NOT EXISTS runtime_runs"
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Transactional persistence helpers for enqueue, transition, and terminal finalization."
      contains: "fn transition_run_stage"
  key_links:
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/src/db.rs
      via: "Runtime state mutations use SQLite connection helpers for atomic writes"
      pattern: "with_connection"
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "Terminal finalizer writes durable run state before queue promotion"
      pattern: "finalize_run"
---

<objective>
Create the persistence foundation for deterministic run state and history.

Purpose: Replace in-memory-only run truth with durable canonical lifecycle records that survive crashes and restarts.
Output: Runtime state schema plus transactional persistence primitives in the runtime boundary.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deterministic-run-state-engine/04-CONTEXT.md
@.planning/phases/04-deterministic-run-state-engine/04-RESEARCH.md
@.planning/phases/03-local-worker-runtime-boundary/03-03-SUMMARY.md
@src-tauri/src/db.rs
@src-tauri/src/runtime_boundary.rs

<interfaces>
From `src-tauri/src/runtime_boundary.rs`:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct RuntimeQueueOutcome {
    pub status: String,
    pub queue_position: Option<usize>,
    pub reason_code: Option<String>,
    pub fix_hint: Option<String>,
}
```

From `src-tauri/src/db.rs`:
```rust
pub fn with_connection<T, F>(db_path: &Path, operation: F) -> Result<T, String>
where
    F: FnOnce(&Connection) -> rusqlite::Result<T>,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SQLite schema for canonical run rows and transition timeline history</name>
  <files>src-tauri/src/db.rs, src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Runtime tables exist for canonical run state and append-only transitions.
    - Canonical stage values support `queued`, `preparing`, `coding`, `validating`, and `publishing`.
    - Terminal metadata stores `success|failed|cancelled|guardrail_blocked` plus optional reason/fix hint.
  </behavior>
  <action>Add runtime persistence schema to `initialize_schema` with explicit indices for repository ordering and issue history lookups. Introduce runtime persistence structs/enums in `runtime_boundary.rs` that map DB rows to lifecycle payloads without changing existing command names. Keep schema backward-compatible with existing app bootstrap behavior.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo check</automated>
  </verify>
  <done>SQLite can represent active/queued runs, transition chronology, and terminal metadata required for restart recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional stage-transition persistence guardrails</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Stage progression is deterministic (`queued -> preparing -> coding -> validating -> publishing -> terminal`) with no skips/backtracking.
    - Every accepted transition updates canonical run row and inserts a transition row atomically.
    - Queue order is stored durably so FIFO ordering is deterministic after restart.
  </behavior>
  <action>Create a single transition function that validates expected current stage before writing next state, executes canonical-row update plus transition insert in one transaction, and returns explicit runtime errors when transition invariants are violated. Route enqueue/start/finalize persistence writes through this function so runtime state has one authoritative write path.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture</automated>
  </verify>
  <done>Invalid stage transitions are rejected and valid transitions are persisted atomically with ordered history entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist terminal metadata and enforce newest-first last-20 history retention</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Terminal finalization writes status, reasonCode, and fixHint metadata to canonical records.
    - History retention keeps newest 20 terminal runs per repository+issue and prunes older terminal rows only.
    - Transition history remains queryable newest-first for details-panel inspection.
  </behavior>
  <action>Update finalization persistence so durable terminal metadata becomes the source of truth, and add pruning logic scoped to terminal rows to retain the latest 20 runs per issue. Add tests for terminal metadata persistence, retention pruning behavior, and chronological transition ordering.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Terminal outcomes are durable, queryable, and bounded to the required inspection window without deleting active/queued rows.</done>
</task>

</tasks>

<verification>
- Runtime schema migrations compile and initialize through normal app startup.
- Stage transitions are atomic and ordered, with invalid progression rejected.
- Terminal history is durable, newest-first, and capped to last 20 per issue.
</verification>

<success_criteria>
- ORCH-02 persistence foundation exists and is deterministic under restart/crash scenarios.
- Runtime state can be reconstructed from SQLite without relying on in-memory queue state alone.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-run-state-engine/04-01-SUMMARY.md`.
</output>
