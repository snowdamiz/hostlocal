---
phase: 05-live-telemetry-and-safe-summaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - src-tauri/src/db.rs
  - src-tauri/src/runtime_boundary.rs
autonomous: true
requirements:
  - OBS-01
  - SEC-01
must_haves:
  truths:
    - "Runtime lifecycle emits live telemetry entries that can power a selected-issue sidebar feed."
    - "Telemetry text is redacted in Rust before both persistence and UI event emission."
    - "Sensitive fragments are masked inline with `[REDACTED]` so milestones remain readable without leaking secrets."
  artifacts:
    - path: src-tauri/src/db.rs
      provides: "SQLite telemetry event store keyed by run and deterministic sequence."
      contains: "CREATE TABLE IF NOT EXISTS runtime_run_events"
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Redaction engine and sanitized telemetry record/emit pipeline."
      contains: "fn redact_sensitive_text"
  key_links:
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/src/db.rs
      via: "Runtime lifecycle paths persist sanitized telemetry rows into runtime_run_events."
      pattern: "INSERT INTO runtime_run_events"
    - from: src-tauri/src/runtime_boundary.rs
      to: src/features/board/hooks/useBoardInteractions.ts
      via: "Rust emits live telemetry over a dedicated channel consumed by board hooks."
      pattern: "runtime/run-telemetry"
---

<objective>
Create the secure backend telemetry foundation for live run activity.

Purpose: Deliver OBS-01 activity signals while enforcing SEC-01 masking guarantees before any telemetry leaves the backend boundary.
Output: Sanitized telemetry persistence schema, redaction rules, and live telemetry event emission from runtime lifecycle transitions.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-CONTEXT.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-RESEARCH.md
@.planning/phases/04-deterministic-run-state-engine/04-02-SUMMARY.md
@.planning/phases/04-deterministic-run-state-engine/04-03-SUMMARY.md
@src-tauri/src/db.rs
@src-tauri/src/runtime_boundary.rs

<interfaces>
From `src-tauri/src/runtime_boundary.rs`:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct RuntimeRunStageChangedEventPayload {
    pub run_id: i64,
    pub repository_full_name: String,
    pub repository_key: String,
    pub issue_number: i64,
    pub issue_title: String,
    pub issue_branch_name: String,
    pub stage: String,
    pub queue_position: Option<usize>,
    pub terminal_status: Option<String>,
    pub reason_code: Option<String>,
    pub fix_hint: Option<String>,
}
```

From `src-tauri/src/db.rs`:
```rust
pub fn initialize_schema(conn: &Connection) -> rusqlite::Result<()>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict telemetry redaction rules in Rust before any UI/data-store boundary</name>
  <files>src-tauri/Cargo.toml, src-tauri/Cargo.lock, src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Token/API key/auth header/cookie/session/credential-like env patterns are detected and masked with `[REDACTED]`.
    - Uncertain-but-risky fragments are conservatively masked rather than passed through.
    - Redaction returns both masked text and redaction metadata for downstream telemetry payloads.
  </behavior>
  <action>Add regex-backed redaction helpers (using compile-once patterns) to `runtime_boundary.rs`, including structured reasons metadata for why masking occurred. Update Cargo dependencies/lockfile for the redaction helpers and add focused Rust tests that prove known secret shapes never survive output. Keep all masking backend-only; do not expose raw variants for UI reveal in this phase.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture</automated>
  </verify>
  <done>Runtime boundary can sanitize telemetry strings deterministically and never emits raw secrets to callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist sanitized telemetry milestones with deterministic per-run ordering</name>
  <files>src-tauri/src/db.rs, src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - SQLite stores telemetry rows per run with monotonic `sequence` and newest-first query index coverage.
    - Stored messages are always already-masked text; raw telemetry text is never written.
    - Rows track `kind`, `stage`, and `include_in_summary` so summary projection can reuse same evidence source.
  </behavior>
  <action>Extend `initialize_schema` with a `runtime_run_events` table and supporting indexes. Add runtime-boundary insert/query helpers that compute next sequence, persist masked text plus redaction metadata, and mark summary-eligible milestones. Keep schema additions backward-compatible with existing runtime tables and startup initialization flow.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Telemetry events are durably queryable in newest-first order and contain only sanitized message content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit milestone-level live telemetry events from runtime lifecycle transitions</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Live telemetry events are produced for queue/start/prep/coding/validation/publishing/finalization milestones.
    - Event payloads are milestone-level and concise (not raw line-by-line command transcript).
    - Emit failures do not block queue progression or runtime finalization.
  </behavior>
  <action>Introduce a dedicated `runtime/run-telemetry` event payload and wire telemetry recording+emission into authoritative lifecycle paths (`enqueue`, stage transitions, sidecar termination/error, recovery finalization, queue promotion). Reuse the redaction helper before persist/emit, preserve existing stage-changed channel behavior, and keep telemetry newest-first by sequence when replayed.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Backend continuously emits sanitized, milestone-level live telemetry suitable for right-sidebar consumption.</done>
</task>

</tasks>

<verification>
- Redaction tests prove sensitive substrings are masked with `[REDACTED]` before persistence and emission.
- `runtime_run_events` schema and indexes initialize successfully on app startup.
- Runtime lifecycle transitions emit live telemetry without regressing queue/finalization behavior.
</verification>

<success_criteria>
- OBS-01 backend feed foundation exists with deterministic event ordering and live emission.
- SEC-01 enforcement is backend-authoritative for telemetry strings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-telemetry-and-safe-summaries/05-01-SUMMARY.md`.
</output>
