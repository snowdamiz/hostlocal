---
phase: 05-live-telemetry-and-safe-summaries
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/lib/commands.ts
  - src/features/board/hooks/useBoardInteractions.ts
  - src/features/board/hooks/useBoardInteractions.test.ts
  - src/features/board/components/IssueDetailsPanel.tsx
  - src/components/MainLayout.tsx
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - SEC-01
must_haves:
  truths:
    - "Selected issue details panel shows newest-first live telemetry updates while a run is active."
    - "Terminal issue view keeps final telemetry entries visible and shows a final summary with completion, key actions, and explicit validation outcomes."
    - "Sidebar displays masked content as `[REDACTED]` and does not provide plaintext reveal paths in this phase."
  artifacts:
    - path: src/lib/commands.ts
      provides: "Typed frontend contracts for telemetry stream replay and final run summaries."
      contains: "runtimeGetIssueRunTelemetry"
    - path: src/features/board/hooks/useBoardInteractions.ts
      provides: "Issue-scoped telemetry and summary hydration plus live event merge."
      contains: "runtime/run-telemetry"
    - path: src/features/board/components/IssueDetailsPanel.tsx
      provides: "Right-sidebar live telemetry feed and final summary rendering in existing issue panel."
      contains: "Live runtime activity"
    - path: src/components/MainLayout.tsx
      provides: "Wiring for new telemetry/summary accessors into IssueDetailsPanel."
      contains: "selectedBoardRuntimeSummary"
  key_links:
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/lib/commands.ts
      via: "Hook hydrates telemetry/summary via typed invoke wrappers and merges runtime live events."
      pattern: "runtimeGetIssueRunSummary"
    - from: src/components/MainLayout.tsx
      to: src/features/board/components/IssueDetailsPanel.tsx
      via: "Main layout passes selected-issue telemetry and summary accessors to sidebar panel."
      pattern: "IssueDetailsPanel"
---

<objective>
Render Phase 5 observability in the existing right sidebar issue-details surface.

Purpose: Deliver live activity visibility and safe terminal summaries where users already inspect issue runtime state.
Output: Frontend typed contracts, hook-level telemetry/summary state, and sidebar UI sections for live feed + final summary.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-CONTEXT.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-RESEARCH.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-02-PLAN.md
@src/lib/commands.ts
@src/features/board/hooks/useBoardInteractions.ts
@src/features/board/hooks/useBoardInteractions.test.ts
@src/features/board/components/IssueDetailsPanel.tsx
@src/components/MainLayout.tsx

<interfaces>
From `src/features/board/hooks/useBoardInteractions.ts`:
```typescript
const selectedBoardRuntime = createMemo(() => { ... });
const selectedBoardRuntimeHistory = createMemo(() => { ... });
```

From `src/features/board/components/IssueDetailsPanel.tsx`:
```typescript
interface IssueDetailsPanelProps {
  selectedBoardItem: Accessor<GithubRepositoryItem | null>;
  selectedBoardRuntime: Accessor<RuntimeRepositoryRunSnapshotItem | null>;
  selectedBoardRuntimeHistory: Accessor<RuntimeIssueRunHistoryItem[]>;
  onClose: () => void;
  onOpenGithubItemPage: (url: string) => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend typed command contracts and board hook state for telemetry + summary data</name>
  <files>src/lib/commands.ts, src/features/board/hooks/useBoardInteractions.ts, src/components/MainLayout.tsx</files>
  <behavior>
    - Frontend has typed contracts for telemetry replay command, summary command, and live telemetry event payload.
    - Board hook hydrates telemetry/summary for selected issue and merges `runtime/run-telemetry` updates with repository filtering.
    - Telemetry feed remains newest-first and follows currently selected issue without cross-repo bleed-through.
  </behavior>
  <action>Add telemetry/summary interfaces and invoke wrappers in `commands.ts`. Extend `useBoardInteractions` with issue-scoped telemetry/summary maps, hydration loaders, and telemetry-event subscription lifecycle alongside existing stage listener behavior. Wire new hook accessors through `MainLayout` into `IssueDetailsPanel`, keeping runtime state ownership in the hook (no component-local fetches).</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/hooks/useBoardInteractions.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Frontend state layer can deliver live telemetry and final summary data to the right sidebar from backend contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand board-hook tests for telemetry filtering, ordering, and terminal handoff semantics</name>
  <files>src/features/board/hooks/useBoardInteractions.test.ts</files>
  <behavior>
    - Telemetry listener ignores non-selected repository events.
    - Newest-first merge behavior is deterministic for repeated events on the same issue.
    - Terminal runs keep final telemetry visible while summary remains available with explicit validation fallback fields.
  </behavior>
  <action>Add unit tests around new helper functions and event-subscription pathways in `useBoardInteractions.test.ts`. Cover selected-issue focus behavior, feed ordering, telemetry retention after terminal status, and summary fallback handling (`not-run`/`not-found`). Keep tests fast and deterministic by mocking `listen` and invoke wrappers, matching existing hook test style.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/hooks/useBoardInteractions.test.ts</automated>
  </verify>
  <done>Telemetry/summary state behavior is locked by tests and safe against regression in selection/listener flows.</done>
</task>

<task type="auto">
  <name>Task 3: Render live telemetry feed and final summary inside IssueDetailsPanel without new navigation surfaces</name>
  <files>src/features/board/components/IssueDetailsPanel.tsx, src/components/MainLayout.tsx</files>
  <action>Extend `IssueDetailsPanel` to include `Live runtime activity` and `Run summary` sections in the existing sidebar flow. Live activity must show concise milestone entries in newest-first order; summary must show completion outcome, key-action bullets, and explicit validation outcome badges including unavailable states. Preserve existing runtime stage/history sections, use Tailwind utility classes with design tokens (no hardcoded colors), and keep masked values visible as `[REDACTED]` with no plaintext reveal toggle in this phase.</action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm test -- src/features/board/hooks/useBoardInteractions.test.ts</automated>
  </verify>
  <done>Users see live activity and safe final summaries in the same right-sidebar issue panel with no extra routing or insecure reveal paths.</done>
</task>

</tasks>

<verification>
- Right sidebar issue panel receives live telemetry updates while runs are active.
- Final summary renders completion, key actions, and explicit validation outcomes when run is terminal.
- UI consumes only sanitized telemetry fields and retains `[REDACTED]` markers intact.
</verification>

<success_criteria>
- OBS-01 is satisfied in the renderer by selected-issue live feed rendering in the existing right sidebar.
- OBS-02 is satisfied by terminal summary rendering in the same sidebar panel.
- SEC-01 holds at UI boundary: no plaintext secret exposure path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-telemetry-and-safe-summaries/05-03-SUMMARY.md`.
</output>
