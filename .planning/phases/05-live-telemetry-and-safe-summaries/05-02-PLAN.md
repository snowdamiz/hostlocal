---
phase: 05-live-telemetry-and-safe-summaries
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src-tauri/src/runtime_boundary.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - SEC-01
must_haves:
  truths:
    - "Frontend can hydrate a selected issue's live telemetry feed from a newest-first backend read command."
    - "Terminal runs expose a structured summary with completion outcome, concise key actions, and explicit validation states."
    - "Summary and telemetry command payloads contain only sanitized text fields."
  artifacts:
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Telemetry read models and summary projection derived from runtime_runs + runtime_run_events."
      contains: "pub struct RuntimeIssueRunSummary"
    - path: src-tauri/src/commands.rs
      provides: "Tauri command handlers for telemetry and summary retrieval."
      contains: "runtime_get_issue_run_telemetry"
    - path: src-tauri/src/lib.rs
      provides: "Handler registration for new telemetry/summary command surfaces."
      contains: "commands::runtime_get_issue_run_summary"
  key_links:
    - from: src-tauri/src/commands.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "Command handlers delegate to runtime-boundary telemetry and summary projection helpers."
      pattern: "runtime_get_issue_run_summary"
    - from: src-tauri/src/runtime_boundary.rs
      to: src/features/board/components/IssueDetailsPanel.tsx
      via: "Summary contract supplies completion, key actions, and explicit validation outcomes for sidebar rendering."
      pattern: "validation"
---

<objective>
Expose backend telemetry and summary command contracts for sidebar hydration.

Purpose: Convert sanitized runtime evidence into explicit OBS-01/OBS-02 read models without leaking secrets.
Output: New commands for selected-issue telemetry replay and final summary projection with deterministic fallback states.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-CONTEXT.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-RESEARCH.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-01-PLAN.md
@src-tauri/src/runtime_boundary.rs
@src-tauri/src/commands.rs
@src-tauri/src/lib.rs

<interfaces>
From `src-tauri/src/runtime_boundary.rs`:
```rust
pub fn runtime_get_repository_run_snapshot(
    db_path: &Path,
    repository_full_name: &str,
) -> Result<RuntimeRepositoryRunSnapshot, String>;
```

```rust
pub fn runtime_get_issue_run_history(
    db_path: &Path,
    request: RuntimeIssueRunHistoryRequest,
) -> Result<RuntimeIssueRunHistory, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add telemetry read contracts for selected-issue hydration</name>
  <files>src-tauri/src/runtime_boundary.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Telemetry command accepts repository + issue identity with optional `runId` and `limit`.
    - Response is newest-first and includes sanitized message, stage, kind, redaction metadata, and timestamps.
    - Repository/issue validation errors return existing runtime-style actionable error text.
  </behavior>
  <action>Define telemetry request/response structs in `runtime_boundary.rs`, add query helpers over `runtime_run_events`, and expose them via new `#[tauri::command]` functions in `commands.rs` plus handler registration in `lib.rs`. Keep payload naming camelCase and align repository filtering behavior with existing runtime snapshot/history commands.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Frontend can request deterministic selected-issue telemetry replay that matches live feed ordering semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project terminal run summaries from canonical runtime state plus telemetry milestones</name>
  <files>src-tauri/src/runtime_boundary.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Summary includes completion outcome/status and terminal timestamp.
    - Summary key actions are concise milestone bullets sourced from summary-eligible telemetry rows.
    - Validation outcomes are always explicit with fallbacks (`not-run` or `not-found`) when absent.
  </behavior>
  <action>Add summary projection types and helpers in `runtime_boundary.rs` that resolve target run (latest by default, optional explicit runId), collect concise key actions from sanitized telemetry events, and derive validation status fields with explicit fallback values. Expose `runtime_get_issue_run_summary` through `commands.rs` and register in `lib.rs`; keep summary in same issue-centric contract boundary (no new view routing concerns).</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Backend returns a stable final summary payload that satisfies OBS-02 without requiring raw transcript access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression tests for ordering, fallback validation states, and secret-safe payload guarantees</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Telemetry query tests enforce newest-first ordering and limit semantics.
    - Summary tests enforce explicit validation fallback states (`not-run`/`not-found`).
    - Security tests assert raw secret fixtures never appear in telemetry/summary payloads.
  </behavior>
  <action>Extend runtime-boundary test coverage with focused fixtures that insert telemetry rows containing sensitive patterns, then verify read models only return masked strings and expected redaction metadata. Add summary projection tests for terminal success/failure with and without validation events, ensuring explicit fallback statuses are returned every time.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture</automated>
  </verify>
  <done>Telemetry and summary APIs are deterministic, explicit, and resistant to secret leakage regressions.</done>
</task>

</tasks>

<verification>
- New telemetry and summary commands compile, are registered, and return typed camelCase payloads.
- Telemetry hydration data is newest-first and bounded by request limit.
- Summary payload always includes completion, key actions, and explicit validation outcome states.
</verification>

<success_criteria>
- OBS-01 command-hydration path exists for selected-issue live feed replay.
- OBS-02 final summary payload contract is available for sidebar rendering.
- SEC-01 remains enforced in command payload content.
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-telemetry-and-safe-summaries/05-02-SUMMARY.md`.
</output>
