---
phase: 02-policy-gated-issue-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/github_intake.rs
  - src-tauri/src/lib.rs
  - src/lib/commands.ts
autonomous: true
requirements:
  - INTK-01
  - INTK-02
must_haves:
  truths:
    - "Eligible Todo issues are accepted only after policy checks and verified GitHub label persistence."
    - "Out-of-policy items are rejected with stable reason codes and actionable fix hints."
    - "GitHub write/persist failures fail closed and never return intake accepted."
  artifacts:
    - path: src-tauri/src/github_intake.rs
      provides: "Backend-authoritative intake policy evaluation and transactional GitHub label persistence."
      contains: "github_attempt_issue_intake"
    - path: src-tauri/src/lib.rs
      provides: "Tauri registration for intake command entrypoint."
      contains: "github_intake::github_attempt_issue_intake"
    - path: src/lib/commands.ts
      provides: "Typed invoke contract for intake attempts and structured outcomes."
      exports:
        - githubAttemptIssueIntake
        - GithubIssueIntakeOutcome
  key_links:
    - from: src/lib/commands.ts
      to: src-tauri/src/lib.rs
      via: "invoke('github_attempt_issue_intake') -> tauri::generate_handler!"
      pattern: "github_attempt_issue_intake"
    - from: src-tauri/src/github_intake.rs
      to: "GitHub REST issues labels endpoints"
      via: "POST label mutation followed by GET verification"
      pattern: "/issues/{issue_number}/labels"
---

<objective>
Create a backend-authoritative intake gate for `Todo -> In Progress` that enforces locked policy decisions and returns deterministic accepted/rejected outcomes.

Purpose: Keep policy truth and GitHub mutation verification in one Rust command boundary so UI cannot accidentally start runs on invalid scope.
Output: New Rust intake module, command registration, and typed frontend invoke wrappers.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-policy-gated-issue-intake/02-CONTEXT.md
@.planning/phases/02-policy-gated-issue-intake/02-RESEARCH.md
@src/lib/commands.ts
@src-tauri/src/github_auth.rs
@src-tauri/src/lib.rs

<interfaces>
From `src/lib/commands.ts`:
```typescript
export interface GithubRepositoryItem {
  id: number;
  number: number;
  state: "open" | "closed";
  isPullRequest: boolean;
  labels: string[];
  body: string | null;
}
```

From `src-tauri/src/lib.rs`:
```rust
.invoke_handler(tauri::generate_handler![
  github_auth::github_list_repository_items,
  // add intake command registration here
])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build policy evaluator + reason taxonomy in Rust with unit coverage</name>
  <files>src-tauri/src/github_intake.rs</files>
  <behavior>
    - Reject unless issue is open, not a pull request, and has non-empty body text.
    - Require intake label prefix-family match (single canonical prefix constant).
    - Reject fail-closed on out-of-scope deny signals (epic/large markers).
    - Return stable `reasonCode` + `fixHint` fields for every rejection path.
  </behavior>
  <action>Create `github_intake.rs` with typed request/outcome structs, a pure `evaluate_issue_intake_policy(...)` function, and table-driven Rust unit tests validating every locked decision from `02-CONTEXT.md`. Include discretionary choices as explicit constants: canonical intake prefix, deny-signal set, and agent label prefix handling.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test github_intake::tests::policy -- --nocapture</automated>
  </verify>
  <done>Policy evaluator is deterministic, tested, and produces structured rejection reasons/hints for all disallowed cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional GitHub intake command with post-write verification</name>
  <files>src-tauri/src/github_intake.rs</files>
  <behavior>
    - Accept path performs policy check, applies required labels, refetches issue, and verifies labels before success.
    - Reject path returns structured reason without triggering any run-start side effects.
    - Label-persistence failure returns fail-closed rejection (`label_persist_failed` or rate-limit-aware equivalent).
  </behavior>
  <action>Add `#[tauri::command] async fn github_attempt_issue_intake(...)` that uses existing GitHub auth/session patterns, fetches authoritative issue data, evaluates policy, persists intake + `agent:*` labels, then refetches and verifies required labels are truly present. Handle GitHub 403/429 responses with retry/backoff-aware error mapping and return explicit rejection outcomes rather than generic strings.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test github_intake -- --nocapture</automated>
  </verify>
  <done>Command returns accepted only when policy and label persistence both succeed, and returns explicit rejection outcomes for all failures.</done>
</task>

<task type="auto">
  <name>Task 3: Expose intake command through Tauri and typed frontend wrappers</name>
  <files>src-tauri/src/lib.rs, src/lib/commands.ts</files>
  <action>Register the new intake command in `tauri::generate_handler!` and add typed TS interfaces/wrappers (`GithubIssueIntakeRequest`, `GithubIssueIntakeOutcome`, `githubAttemptIssueIntake`) using camelCase payloads. Keep wrapper signatures explicit so `MainLayout` can consume reason codes/hints without ad-hoc parsing.</action>
  <verify>
    <automated>pnpm build &amp;&amp; (cd src-tauri &amp;&amp; cargo check)</automated>
  </verify>
  <done>Frontend can call a typed `githubAttemptIssueIntake` command and backend builds with registered handler.</done>
</task>

</tasks>

<verification>
- Rust policy tests cover accept and reject branches for all locked intake rules.
- `cargo check` succeeds with new command registration.
- Frontend build succeeds with typed intake invoke wrapper.
</verification>

<success_criteria>
- Backend is the sole authority for intake acceptance/rejection outcomes.
- Every rejection returns explicit `reasonCode` + `fixHint`.
- Intake acceptance is impossible without verified GitHub label persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-policy-gated-issue-intake/02-01-SUMMARY.md`.
</output>
