---
phase: 02-policy-gated-issue-intake
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/intake/intake-state.ts
  - src/components/MainLayout.tsx
autonomous: true
requirements:
  - INTK-01
  - INTK-02
must_haves:
  truths:
    - "Only completed Todo -> In Progress drops trigger intake attempts."
    - "Duplicate starts for the same issue are blocked while prior intake outcome is unresolved."
    - "Accepted intake starts the run boundary and refreshes board state from GitHub truth."
    - "Rejected intake reverts In Progress intent and surfaces the global rejection toast."
  artifacts:
    - path: src/intake/intake-state.ts
      provides: "Per-issue intake pending/outcome guard helpers."
      exports:
        - createIntakeAttemptState
        - beginIntakeAttempt
        - resolveIntakeAttempt
    - path: src/components/MainLayout.tsx
      provides: "Policy-gated drop trigger, drag-handle behavior, rejection rollback, and accepted-path run start hook."
      contains: "handleColumnDrop"
  key_links:
    - from: src/components/MainLayout.tsx
      to: src/lib/commands.ts
      via: "githubAttemptIssueIntake call from drop handler"
      pattern: "githubAttemptIssueIntake"
    - from: src/components/MainLayout.tsx
      to: src/intake/toast-store.ts
      via: "rejection reason emission to global toast"
      pattern: "pushIntakeRejectionToast"
    - from: src/components/MainLayout.tsx
      to: src/components/MainLayout.tsx
      via: "accepted outcome -> startAgentRun boundary -> loadRepositoryItems refresh"
      pattern: "startAgentRun"
---

<objective>
Wire the board interaction path so `Todo -> In Progress` movement becomes a policy-gated intake transaction instead of a local column reassignment.

Purpose: Deliver the user-visible Phase 2 behavior where accepted intake starts a run boundary and rejected intake is clearly explained without background side effects.
Output: Dedicated drag-handle interactions, guarded drop logic, intake pending guard state, and acceptance/rejection flow in `MainLayout`.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-policy-gated-issue-intake/02-CONTEXT.md
@.planning/phases/02-policy-gated-issue-intake/02-RESEARCH.md
@.planning/phases/02-policy-gated-issue-intake/02-01-SUMMARY.md
@.planning/phases/02-policy-gated-issue-intake/02-02-SUMMARY.md
@src/components/MainLayout.tsx
@src/lib/commands.ts
@src/intake/toast-store.ts
@src/intake/policy-reasons.ts

<interfaces>
From `src/components/MainLayout.tsx`:
```typescript
const handleCardDragStart = (event: DragEvent, itemId: number) => { ... };
const handleColumnDrop = (event: DragEvent, columnKey: KanbanColumnKey) => { ... };
```

From `src/lib/commands.ts` (from Plan 02-01):
```typescript
export interface GithubIssueIntakeOutcome {
  accepted: boolean;
  reasonCode: string | null;
  fixHint: string | null;
}
export function githubAttemptIssueIntake(...): Promise<GithubIssueIntakeOutcome>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce intake-attempt state guard and drag-handle interaction model</name>
  <files>src/intake/intake-state.ts, src/components/MainLayout.tsx</files>
  <behavior>
    - Card body remains clickable for selection/details.
    - Drag starts only from dedicated drag handle.
    - Active drag interactions suppress accidental text selection on canvas/card surfaces.
  </behavior>
  <action>Create `intake-state.ts` to track unresolved attempts per issue and expose helper functions for begin/resolve checks. Update `MainLayout` card markup to use a dedicated drag handle element (only draggable node) while preserving existing click-to-select behavior on the card body. Add temporary drag-state class toggles needed to prevent accidental text highlighting during drag.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Drag intent and click intent are separated, and unresolved attempts can be guarded by issue ID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace local drop reassignment with policy-gated Todo -> In Progress transaction</name>
  <files>src/components/MainLayout.tsx</files>
  <behavior>
    - Intake trigger runs only on completed drop into `inProgress` from `todo`.
    - Drag-over never triggers intake.
    - Rejections do not start runs and card reverts to Todo integrity state.
  </behavior>
  <action>Refactor `handleColumnDrop` to resolve source column from GitHub-truth grouping, short-circuit non-`todo -> inProgress` drops, and call `githubAttemptIssueIntake` only for eligible transitions. Use intake-state guard to block duplicate attempts while pending. On rejection/error, emit `pushIntakeRejectionToast(reasonCode, fixHint)` and clear any optimistic in-progress intent so board integrity remains Todo for rejected items.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Drop handler enforces locked trigger semantics and rejection behavior without local-only in-progress drift.</done>
</task>

<task type="auto">
  <name>Task 3: Start run boundary on acceptance and refresh GitHub-authoritative board state</name>
  <files>src/components/MainLayout.tsx</files>
  <action>After accepted intake, invoke a dedicated `startAgentRunForIssue(...)` boundary in `MainLayout` (temporary minimal contract for Phase 3 integration) and then refresh repository items from GitHub so `In Progress` is derived from persisted labels, not `manualColumnByItemId`. Keep acceptance path side-effect order explicit: intake accepted -> start boundary call -> GitHub refresh.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Eligible moves automatically invoke run-start boundary and board state is reconciled from GitHub-truth labels.</done>
</task>

</tasks>

<verification>
- `Todo -> In Progress` is the only drag/drop path that can initiate intake.
- Duplicate drop attempts for the same issue while pending do not trigger second starts.
- Rejections surface deduplicated global toasts and do not continue into run-start.
- Accepted path calls run-start boundary and reloads board state from GitHub.
</verification>

<success_criteria>
- Phase 2 board behavior is policy-gated, not local-state-gated.
- User-visible rejection guidance and rollback behavior are consistent and deterministic.
- INTK-01 and INTK-02 are both satisfied through real drop-path wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/02-policy-gated-issue-intake/02-03-SUMMARY.md`.
</output>
