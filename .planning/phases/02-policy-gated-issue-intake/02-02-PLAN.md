---
phase: 02-policy-gated-issue-intake
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intake/policy-reasons.ts
  - src/intake/toast-store.ts
  - src/components/IntakeToastViewport.tsx
  - src/App.tsx
  - src/App.css
autonomous: true
requirements:
  - INTK-02
must_haves:
  truths:
    - "Policy rejections are surfaced through a global toast with violated rule and actionable fix hint."
    - "Repeated identical rejection attempts collapse into a visible counter rather than duplicated toast stacks."
    - "Toast rendering is app-wide and can be triggered from board interaction code."
  artifacts:
    - path: src/intake/policy-reasons.ts
      provides: "Reason-code catalog and user-facing rejection copy contract."
      exports:
        - INTAKE_POLICY_REASON_MAP
        - resolveIntakePolicyReason
    - path: src/intake/toast-store.ts
      provides: "Global deduplicating toast state and APIs."
      exports:
        - createIntakeToastStore
        - pushIntakeRejectionToast
    - path: src/components/IntakeToastViewport.tsx
      provides: "Accessible toast viewport for policy outcomes."
      contains: "counter rendering + auto-dismiss timing"
    - path: src/App.tsx
      provides: "Global toast viewport mount so any flow can emit rejections."
      contains: "IntakeToastViewport"
  key_links:
    - from: src/intake/policy-reasons.ts
      to: src/intake/toast-store.ts
      via: "reasonCode -> copy/hint resolution before enqueue"
      pattern: "resolveIntakePolicyReason"
    - from: src/App.tsx
      to: src/components/IntakeToastViewport.tsx
      via: "top-level render mount in app shell"
      pattern: "<IntakeToastViewport"
---

<objective>
Create the global rejection UX layer required by intake policy gating, including explicit messages and duplicate-attempt collapse behavior.

Purpose: Ensure users always understand why intake was rejected and how to fix it, without toast noise from repeated failed attempts.
Output: Shared reason map, dedupe-aware toast store, and globally mounted toast viewport.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-policy-gated-issue-intake/02-CONTEXT.md
@.planning/phases/02-policy-gated-issue-intake/02-RESEARCH.md
@src/App.tsx
@src/App.css
@src/components/MainLayout.tsx

<interfaces>
From `src/App.tsx`:
```tsx
<main class={`app ${setupStatus() === "ready" ? "app-main" : "app-onboarding"}`}>
  <WindowControls />
  <Switch>...</Switch>
</main>
```

From `src/components/MainLayout.tsx`:
```typescript
const formatInvokeError = (error: unknown, fallback: string) => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define intake rejection reason contract and hint resolution module</name>
  <files>src/intake/policy-reasons.ts</files>
  <behavior>
    - Reason codes are stable keys shared with backend outcomes.
    - Every reason resolves to "violated rule" text plus one actionable fix hint.
    - Unknown codes fall back to a safe generic policy rejection message.
  </behavior>
  <action>Create `policy-reasons.ts` with a typed reason map matching Phase 2 outcomes (`missing_intake_label`, `empty_body`, `is_pull_request`, `deny_signal_present`, `label_persist_failed`, duplicate-pending guard code). Include helper functions returning normalized display payloads consumed by the toast store.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Reason-to-message mapping is centralized, typed, and ready for UI consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deduplicating global toast store with counter-reset window</name>
  <files>src/intake/toast-store.ts</files>
  <behavior>
    - Repeated identical rejection events within configured window increment count.
    - New reason key or expired window creates a fresh toast entry.
    - Store supports clear/dismiss operations for viewport lifecycle control.
  </behavior>
  <action>Build a small global store (signal-backed or equivalent project pattern) keyed by rejection signature. Implement counter merge logic, timestamp tracking, and reset window (discretionary constant). Expose narrow APIs so `MainLayout` can emit toasts without duplicating collapse logic.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Toast store deterministically collapses repeated rejections and exposes a minimal API for producers/consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Add globally mounted intake toast viewport with token-driven styling</name>
  <files>src/components/IntakeToastViewport.tsx, src/App.tsx, src/App.css</files>
  <action>Create `IntakeToastViewport` with accessible toast semantics (`role="status"`/`aria-live`) and render collapsed attempt count when `count &gt; 1`. Mount viewport at the app shell level in `App.tsx`. If Tailwind v4 utilities are available in this branch, use them for layout/spacing; otherwise use existing tokenized CSS variables in `App.css` and avoid introducing hardcoded color values.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Toast UI is globally visible, token-consistent, and ready for Phase 2 intake rejection events.</done>
</task>

</tasks>

<verification>
- Repeated identical rejection events render a single toast row with incrementing counter.
- Rejection copy always includes violated-rule context and actionable fix hint.
- App shell compiles with globally mounted toast viewport.
</verification>

<success_criteria>
- INTK-02 messaging infrastructure exists before board intake wiring.
- Toast behavior prevents duplicate spam while preserving rejection visibility.
- Styling changes remain token-driven and do not introduce new hardcoded colors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-policy-gated-issue-intake/02-02-SUMMARY.md`.
</output>
