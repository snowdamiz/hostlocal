---
phase: 03-local-worker-runtime-boundary
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/lib/commands.ts
  - src/features/board/hooks/useBoardInteractions.ts
  - src/intake/policy-reasons.ts
  - src/intake/policy-reasons.test.ts
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - SEC-02
must_haves:
  truths:
    - "Accepted `todo -> inProgress` intake now calls the runtime enqueue command through typed invoke wrappers."
    - "Runtime blocked/startup-failure outcomes immediately surface rejection toasts and move the issue back to Todo."
    - "If an in-progress issue is moved back to Todo before run start, queued runtime work is removed for that issue."
    - "Runtime guardrail failures never auto-retry and keep In Progress column trust intact."
  artifacts:
    - path: src/lib/commands.ts
      provides: "Typed runtime enqueue/dequeue request and outcome invoke wrappers."
      contains: "runtimeEnqueueIssueRun"
    - path: src/features/board/hooks/useBoardInteractions.ts
      provides: "Board seam integration for runtime enqueue/dequeue and guardrail rollback behavior."
      contains: "startAgentRunForIssue"
    - path: src/intake/policy-reasons.ts
      provides: "Runtime guardrail/startup reason catalog for global rejection-toast copy."
      contains: "runtime_guardrail"
  key_links:
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/lib/commands.ts
      via: "start run and dequeue operations route through typed runtime command wrappers"
      pattern: "runtimeEnqueueIssueRun"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/intake/toast-store.ts
      via: "runtime rejection outcomes emitted via global rejection-toast pipeline"
      pattern: "pushIntakeRejectionToast"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src-tauri/src/runtime_boundary.rs
      via: "queue removal on In Progress -> Todo reversion"
      pattern: "runtimeDequeueIssueRun"
---

<objective>
Integrate the frontend board run-start seam with the new runtime boundary while preserving current intake drag semantics and rejection UX patterns.

Purpose: Ensure queue/start/guardrail outcomes are visible, deterministic, and trustworthy in existing board interactions.
Output: Typed runtime invoke wrappers, board seam runtime wiring, and runtime reason-code mapping/tests for rejection toasts.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-local-worker-runtime-boundary/03-CONTEXT.md
@.planning/phases/03-local-worker-runtime-boundary/03-RESEARCH.md
@.planning/phases/03-local-worker-runtime-boundary/03-01-PLAN.md
@src/lib/commands.ts
@src/features/board/hooks/useBoardInteractions.ts
@src/intake/policy-reasons.ts
@src/intake/toast-store.ts

<interfaces>
From `src/features/board/hooks/useBoardInteractions.ts`:
```typescript
const startAgentRunForIssue = async (item: GithubRepositoryItem) => { ... };
const performColumnDrop = async (itemId: number, columnKey: KanbanColumnKey, dragContext?: PointerDragContext) => { ... };
```

From `src/lib/commands.ts`:
```typescript
export interface GithubIssueIntakeOutcome {
  accepted: boolean;
  reasonCode: string | null;
  fixHint: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed runtime enqueue/dequeue invoke contracts</name>
  <files>src/lib/commands.ts</files>
  <action>Define runtime request/outcome interfaces and status unions for enqueue/dequeue commands, then add typed wrappers (`runtimeEnqueueIssueRun`, `runtimeDequeueIssueRun`) that call the new Tauri command names from Plan 03-01. Keep naming/serialization aligned with Rust camelCase payloads.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Frontend has explicit typed command wrappers for runtime start/dequeue behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `startAgentRunForIssue` to runtime enqueue outcomes with guardrail rollback</name>
  <files>src/features/board/hooks/useBoardInteractions.ts, src/features/board/hooks/useBoardInteractions.test.ts</files>
  <behavior>
    - Accepted intake with runtime status `started` or `queued` keeps the issue in In Progress.
    - Runtime blocked/startup-failed outcomes emit rejection toast and move issue back to Todo.
    - Runtime failures do not auto-retry.
  </behavior>
  <action>Replace the run-start placeholder in `startAgentRunForIssue` with `runtimeEnqueueIssueRun` invocation and explicit outcome handling. For blocked/startup-failure outcomes, emit rejection toast with runtime reason code/fix hint and invoke `githubRevertIssueIntake` so board truth returns to Todo. Add/extend `useBoardInteractions.test.ts` coverage to assert blocked/startup-failure outcomes trigger rejection toast emission and rollback invocation, then keep existing drag semantics and duplicate intake guard logic unchanged.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/hooks/useBoardInteractions.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Run start boundary is real, deterministic, and consistent with existing rejection UX expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove queued runs on In Progress -> Todo reverts and extend runtime reason catalog tests</name>
  <files>src/features/board/hooks/useBoardInteractions.ts, src/intake/policy-reasons.ts, src/intake/policy-reasons.test.ts</files>
  <behavior>
    - In Progress -> Todo reversion removes queued-not-started runtime entry for that issue.
    - Runtime guardrail reasons map to violated-rule + fix-hint toast copy.
    - Unknown runtime reason codes still degrade safely to fallback copy.
  </behavior>
  <action>In the revert path, call `runtimeDequeueIssueRun` for the current repository/issue and treat dequeue failures as explicit rejection outcomes (no silent ignore). Add runtime reason codes to `policy-reasons.ts` (guardrail command/path blocks, startup failure, queue removal failure) and extend tests to lock copy/fallback behavior for the new codes.</action>
  <verify>
    <automated>pnpm exec vitest run src/intake/policy-reasons.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Queued-run removal behavior and runtime rejection messaging are enforced through typed flows and tests.</done>
</task>

</tasks>

<verification>
- Board run-start seam calls runtime enqueue wrapper instead of logging placeholder output.
- Blocked runtime outcomes trigger rejection toasts and issue reversion to Todo.
- In Progress -> Todo path removes queued runtime entries for not-yet-started runs.
- Runtime reason catalog/tests cover new reason codes and fallback behavior.
</verification>

<success_criteria>
- Frontend runtime integration preserves trusted board semantics and explicit user feedback.
- Queue removal and runtime guardrail outcomes are visible and deterministic.
- RUN-01/RUN-02/RUN-03/SEC-02 coverage reaches the board interaction boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-worker-runtime-boundary/03-03-SUMMARY.md`.
</output>
