---
phase: 03-local-worker-runtime-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/runtime_boundary.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - RUN-02
  - SEC-02
must_haves:
  truths:
    - "Runtime boundary accepts intake-approved issue runs through Rust commands and returns deterministic `started` or `queued` outcomes."
    - "Only one active run can exist per normalized repository key, with FIFO queueing for additional accepted issues."
    - "Queued runs can be removed by issue identity when users move cards back to Todo before execution begins."
  artifacts:
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Runtime queue state, enqueue/dequeue contracts, and deterministic repo/branch identity helpers."
      contains: "pub async fn runtime_enqueue_issue_run"
    - path: src-tauri/src/lib.rs
      provides: "Tauri command registration for runtime enqueue/dequeue boundaries."
      contains: "runtime_boundary::runtime_enqueue_issue_run"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "invoke_handler registration for runtime boundary commands"
      pattern: "runtime_boundary::runtime_enqueue_issue_run"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src-tauri/src/runtime_boundary.rs
      via: "Phase 3 run-start seam consumes runtime enqueue/dequeue outcomes"
      pattern: "startAgentRunForIssue"
---

<objective>
Establish the Rust-owned runtime command boundary and queue state model that Phase 3 builds on.

Purpose: Replace the run-start placeholder with deterministic backend contracts for per-repository queueing and dequeue-on-revert behavior.
Output: New `runtime_boundary` module with queue contracts/helpers/tests and registered Tauri commands.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-local-worker-runtime-boundary/03-CONTEXT.md
@.planning/phases/03-local-worker-runtime-boundary/03-RESEARCH.md
@.planning/phases/02-policy-gated-issue-intake/02-03-SUMMARY.md
@src/features/board/hooks/useBoardInteractions.ts
@src-tauri/src/lib.rs
@src-tauri/src/github_intake.rs

<interfaces>
From `src/features/board/hooks/useBoardInteractions.ts`:
```typescript
const startAgentRunForIssue = async (item: GithubRepositoryItem) => { ... };
```

From `src-tauri/src/github_intake.rs`:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct GithubIssueIntakeOutcome {
    pub accepted: bool,
    pub reason_code: Option<String>,
    pub fix_hint: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define runtime queue contracts and deterministic identity helpers with unit tests</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Repository keys are normalized to a stable lowercase `owner/repo` key for queue indexing.
    - Per-run branch names follow `hostlocal/issue-&lt;number&gt;-&lt;slug&gt;` with sanitized slug output.
    - Queue operations can remove a queued (not active) run by repository + issue identity.
  </behavior>
  <action>Create `runtime_boundary.rs` with request/outcome structs for enqueue/dequeue commands, queue-state structs (`RuntimeBoundaryState`, per-repo active + FIFO queue), and helper functions for repo-key normalization and deterministic issue-branch naming. Add table-driven unit tests for key normalization, branch slug sanitization, FIFO ordering, and queued-item removal semantics.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture</automated>
  </verify>
  <done>Runtime boundary contracts and queue identity rules are codified and protected by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement enqueue/dequeue Tauri commands with deterministic queue outcomes</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - First accepted run for a repository is marked `started`.
    - Additional accepted runs for the same repository are returned as `queued` with FIFO position.
    - Dequeue removes queued-not-started runs for In Progress -> Todo reverts without mutating active run.
  </behavior>
  <action>Add `#[tauri::command]` handlers for `runtime_enqueue_issue_run` and `runtime_dequeue_issue_run` that use `tauri::State` shared runtime state. Ensure state-lock scope only wraps queue mutation and never spans long-running async operations. Return structured outcomes (`started|queued|not_found|removed`) with explicit `reasonCode/fixHint` fields for downstream toast handling.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture</automated>
  </verify>
  <done>Frontend can invoke runtime enqueue/dequeue and receive deterministic outcomes for queue control flows.</done>
</task>

<task type="auto">
  <name>Task 3: Register runtime boundary module and command handlers in app bootstrap</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/runtime_boundary.rs</files>
  <action>Wire the new module into `lib.rs` (`mod runtime_boundary;`) and register both runtime commands in `tauri::generate_handler!` so they are callable from typed frontend invoke wrappers in later plans.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo check</automated>
  </verify>
  <done>Runtime boundary commands are compiled, registered, and ready for sidecar/workspace execution implementation.</done>
</task>

</tasks>

<verification>
- `runtime_enqueue_issue_run` and `runtime_dequeue_issue_run` compile and are registered in `src-tauri/src/lib.rs`.
- Runtime queue unit tests pass for key normalization, branch naming, FIFO queueing, and queued-item removal.
- No long-running operation is performed while holding the queue mutex.
</verification>

<success_criteria>
- Per-repo active/queued state model exists with deterministic outcomes.
- Dequeue-on-revert behavior is available for queued issues.
- RUN-02 and SEC-02 foundation contracts are established for execution and UI integration plans.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-worker-runtime-boundary/03-01-SUMMARY.md`.
</output>
