---
phase: 03-local-worker-runtime-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/tauri.conf.json
  - src-tauri/src/lib.rs
  - src-tauri/src/runtime_boundary.rs
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - SEC-02
must_haves:
  truths:
    - "Started runs execute through the local Rust/Tauri sidecar path only (no frontend-supplied command strings)."
    - "Each started run creates a fresh workspace in system temp, clones the repository, and creates a deterministic issue branch."
    - "Workspace cleanup runs on all terminal paths (success, failure, cancel, guardrail block) while minimal run evidence persists outside workspace."
    - "Out-of-bound command/path attempts are blocked with explicit reason outcomes that include violated rule + target type without raw sensitive values."
  artifacts:
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Sidecar execution pipeline, workspace guardrails, and terminal-state finalization with cleanup."
      contains: "ensure_within_workspace"
    - path: src-tauri/Cargo.toml
      provides: "Runtime dependencies for shell sidecar execution and ephemeral temp workspace lifecycle."
      contains: "tauri-plugin-shell"
    - path: src-tauri/capabilities/default.json
      provides: "Explicit sidecar execute/spawn permission scope for approved runtime command identity."
      contains: "shell:allow-execute"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "runtime module uses shell plugin initialized in app builder"
      pattern: ".plugin(tauri_plugin_shell::init())"
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/capabilities/default.json
      via: "runtime sidecar alias must match capability allowlist entry"
      pattern: "hostlocal-worker"
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/tauri.conf.json
      via: "sidecar binary name aligns with externalBin declaration"
      pattern: "externalBin"
---

<objective>
Implement the actual local runtime boundary: sidecar-only execution in isolated temp workspaces with strict command/path guardrails and guaranteed cleanup.

Purpose: Fulfill the core Phase 3 runtime and security behaviors so accepted runs are locally executed, isolated, and bounded.
Output: Sidecar execution plumbing, guardrail enforcement, workspace lifecycle management, and cleanup/evidence finalization.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-local-worker-runtime-boundary/03-CONTEXT.md
@.planning/phases/03-local-worker-runtime-boundary/03-RESEARCH.md
@.planning/phases/03-local-worker-runtime-boundary/03-01-PLAN.md
@src-tauri/src/runtime_boundary.rs
@src-tauri/src/lib.rs
@src-tauri/capabilities/default.json
@src-tauri/tauri.conf.json
@src-tauri/Cargo.toml

<interfaces>
From `src-tauri/src/runtime_boundary.rs` (Plan 03-01 output):
```rust
#[tauri::command]
pub async fn runtime_enqueue_issue_run(...) -> Result<RuntimeEnqueueIssueRunOutcome, String>;

#[tauri::command]
pub async fn runtime_dequeue_issue_run(...) -> Result<RuntimeDequeueIssueRunOutcome, String>;
```

From `src-tauri/src/lib.rs`:
```rust
tauri::Builder::default()
  .invoke_handler(tauri::generate_handler![ ... ]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidecar runtime dependencies and explicit capability scope wiring</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/lib.rs, src-tauri/capabilities/default.json, src-tauri/tauri.conf.json</files>
  <action>Add `tauri-plugin-shell` and `tempfile` dependencies, initialize the shell plugin in app bootstrap, and define explicit sidecar command permissions in `capabilities/default.json` for execute/spawn. Add sidecar binary contract metadata to `tauri.conf.json` (`externalBin`) using one stable worker alias so runtime code/config stay aligned.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo check</automated>
  </verify>
  <done>Runtime shell plugin and permission scaffolding are configured for guarded local sidecar execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement guarded workspace preparation and sidecar execution path</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Every started run allocates a new temp workspace under system temp and performs a fresh clone.
    - Deterministic issue branch creation uses `hostlocal/issue-&lt;number&gt;-&lt;slug&gt;` before sidecar execution.
    - Sidecar execution uses an approved alias only and runs with workspace as `current_dir`.
    - Guardrail failures report violated rule + blocked target type without exposing raw command/path values.
  </behavior>
  <action>Implement run-worker logic in `runtime_boundary.rs` that prepares workspace via `TempDir::new_in(std::env::temp_dir())`, clones repository into that workspace, creates deterministic issue branch, and starts the worker via `tauri_plugin_shell` sidecar API only. Add canonical path boundary checks (`canonicalize` + `strip_prefix`) before any filesystem-sensitive target usage, map guardrail blocks to explicit reason codes/fix hints, and keep command/path detail redacted in user-facing strings.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture</automated>
  </verify>
  <done>Started runs execute locally in isolated workspaces with explicit sidecar and path guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Centralize terminal finalization for cleanup, queue handoff, and lightweight run evidence</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Success/failure/cancel/guardrail terminal paths all run the same finalizer.
    - Workspace directory removal is attempted deterministically on all terminal paths.
    - Next queued run for the repository starts FIFO after active-run finalization.
    - Minimal run evidence survives outside workspace for later phases.
  </behavior>
  <action>Create one terminal finalization path that records a lightweight run evidence artifact outside the ephemeral workspace (for example: run id, repository key, issue number, branch, terminal status, blocked reason if any), closes/removes temp workspace, clears active state, and schedules the next queued run for that repo. Add tests for queue handoff and cleanup-path invocation coverage.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>RUN-01/RUN-02/RUN-03/SEC-02 runtime behavior is implemented with deterministic finalization and queue progression.</done>
</task>

</tasks>

<verification>
- Sidecar execution is only reachable through configured Tauri shell plugin permissions and approved alias.
- Run prep creates fresh temp workspace + deterministic issue branch per accepted run.
- Guardrail outcomes are explicit and redacted.
- Cleanup/finalizer executes across all terminal outcomes and starts next queued item FIFO.
</verification>

<success_criteria>
- Local sidecar runtime boundary is fully operational and scoped.
- Workspace isolation and cleanup are deterministic.
- Command/path guardrails satisfy SEC-02 with explicit blocked outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-worker-runtime-boundary/03-02-SUMMARY.md`.
</output>
