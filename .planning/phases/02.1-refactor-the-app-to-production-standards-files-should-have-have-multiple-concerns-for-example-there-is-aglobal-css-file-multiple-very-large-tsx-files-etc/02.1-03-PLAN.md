---
phase: 02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc
plan: 03
type: execute
wave: 2
depends_on:
  - 02.1-02
files_modified:
  - src/features/auth/hooks/useGithubAuth.ts
  - src/features/auth/components/GithubAuthPanel.tsx
  - src/features/repositories/hooks/useRepositories.ts
  - src/features/repositories/components/RepositorySidebar.tsx
  - src/components/MainLayout.tsx
autonomous: true
requirements:
  - STYLE-01
must_haves:
  truths:
    - "Users can still connect GitHub, complete device-flow polling, and sign out without auth flow regressions."
    - "Users continue to see repository loading, empty, and error states, and can select repositories as before."
    - "Auth and repository sidebar interactions remain behaviorally stable after modular extraction from MainLayout."
  artifacts:
    - path: src/features/auth/hooks/useGithubAuth.ts
      provides: "Auth state machine and device-flow orchestration for GitHub connection."
      exports:
        - useGithubAuth
    - path: src/features/auth/components/GithubAuthPanel.tsx
      provides: "Auth footer UI (connect, pending flow, signed-in user) decoupled from board rendering."
      contains: "Connect GitHub"
    - path: src/features/repositories/hooks/useRepositories.ts
      provides: "Repository list/resource loading and selected repository state."
      exports:
        - useRepositories
    - path: src/features/repositories/components/RepositorySidebar.tsx
      provides: "Repository list panel rendering with loading/error/selection states."
      contains: "sidebar-repository-item"
  key_links:
    - from: src/features/auth/hooks/useGithubAuth.ts
      to: src/lib/commands.ts
      via: "githubAuthStart/poll/status/logout command calls"
      pattern: "githubAuth"
    - from: src/features/repositories/hooks/useRepositories.ts
      to: src/lib/commands.ts
      via: "githubListRepositories integration"
      pattern: "githubListRepositories"
    - from: src/components/MainLayout.tsx
      to: src/features/auth/components/GithubAuthPanel.tsx
      via: "composition of extracted auth UI"
      pattern: "<GithubAuthPanel"
---

<objective>
Extract auth and repository concerns from `MainLayout.tsx` into feature hooks/components while preserving the existing behavior from Phase 2.

Purpose: Remove major cross-cutting concerns from the monolithic layout file so board-specific refactors can proceed independently.
Output: Auth feature module, repository feature module, and slimmer MainLayout orchestrator.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-RESEARCH.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-02-SUMMARY.md
@src/components/MainLayout.tsx
@src/lib/commands.ts

<interfaces>
From `src/lib/commands.ts`:
```typescript
export function githubAuthStatus(): Promise<GithubAuthStatus>;
export function githubAuthStart(): Promise<GithubDeviceAuthStart>;
export function githubAuthPoll(): Promise<GithubDeviceAuthPoll>;
export function githubAuthLogout(): Promise<void>;
export function githubListRepositories(): Promise<GithubRepository[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract GitHub auth flow state and actions into a dedicated hook</name>
  <files>src/features/auth/hooks/useGithubAuth.ts, src/components/MainLayout.tsx</files>
  <action>Create `useGithubAuth` to own auth status refresh, device-flow polling timers, connect/start behavior, copy-code state, and logout behavior currently in `MainLayout`. Keep command usage and user-facing error semantics equivalent to current behavior.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Auth orchestration logic is removed from MainLayout and provided through hook return contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Extract repository loading/selection into feature hook and sidebar component</name>
  <files>src/features/repositories/hooks/useRepositories.ts, src/features/repositories/components/RepositorySidebar.tsx, src/components/MainLayout.tsx</files>
  <action>Create `useRepositories` for repository list loading, selection state, and related error/loading flags. Create `RepositorySidebar` presentational component that renders list states and selection UI. MainLayout should consume both while preserving existing selection and empty-state behavior.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Repository concerns are feature-owned and MainLayout no longer renders repository list internals directly.</done>
</task>

<task type="auto">
  <name>Task 3: Add extracted auth panel component and recompose MainLayout shell</name>
  <files>src/features/auth/components/GithubAuthPanel.tsx, src/components/MainLayout.tsx</files>
  <action>Create `GithubAuthPanel` for connect/pending/user footer rendering and wire it into MainLayout using `useGithubAuth` outputs. Keep existing copy/actions text and command side effects unchanged while reducing MainLayout JSX footprint.</action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run src/intake/policy-reasons.test.ts src/intake/toast-store.test.ts</automated>
  </verify>
  <done>MainLayout composes extracted auth UI and existing intake utility tests still pass.</done>
</task>

</tasks>

<verification>
- Auth connect/poll/logout flow still works with extracted hook/component boundaries.
- Repository list loading and selection still render and update correctly.
- MainLayout no longer embeds auth/repository implementation details inline.
</verification>

<success_criteria>
- MainLayout concern count is reduced by moving auth and repository ownership into feature modules.
- Existing Phase 2 behavior remains stable after decomposition.
- File boundaries now reflect domain ownership rather than one giant component.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-03-SUMMARY.md`.
</output>
