---
phase: 02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc
plan: 06
type: execute
wave: 5
depends_on:
  - 02.1-05
files_modified:
  - src/components/MainLayout.tsx
  - src/features/repositories/components/RepositorySidebar.tsx
  - src/features/auth/components/GithubAuthPanel.tsx
  - src/styles/legacy-bridge.css
autonomous: true
gap_closure: true
requirements:
  - STYLE-01
must_haves:
  truths:
    - "Ready-state layout/sidebar/auth UI is styled with Tailwind v4 token-driven classes instead of legacy bridge selectors."
    - "legacy-bridge.css is reduced to residual compatibility scope and explicitly marks platform-specific compatibility selectors."
    - "Post-migration lint, test, and build checks pass without regressions."
  artifacts:
    - path: src/components/MainLayout.tsx
      provides: "Tailwind-driven shell layout without `.layout`/`.sidebar-left` class hooks."
      contains: "classList={{"
    - path: src/features/repositories/components/RepositorySidebar.tsx
      provides: "Token-driven Tailwind repository sidebar rendering without `sidebar-*` selectors."
      contains: "Connect GitHub to see your repositories."
    - path: src/features/auth/components/GithubAuthPanel.tsx
      provides: "Token-driven Tailwind auth footer/device/user controls without `sidebar-*` selectors."
      contains: "Connect GitHub"
    - path: src/styles/legacy-bridge.css
      provides: "Residual compatibility CSS with explicit platform marker section."
      contains: "Platform-specific compatibility selectors"
  key_links:
    - from: src/components/MainLayout.tsx
      to: src/features/repositories/components/RepositorySidebar.tsx
      via: "left-rail composition for repository selection"
      pattern: "<RepositorySidebar"
    - from: src/components/MainLayout.tsx
      to: src/features/auth/components/GithubAuthPanel.tsx
      via: "left-rail composition for auth controls"
      pattern: "<GithubAuthPanel"
    - from: src/styles/legacy-bridge.css
      to: src/components/WindowControls.tsx
      via: "platform pseudo/content compatibility selectors retained in bridge"
      pattern: "data-platform"
---

<objective>
Close the remaining STYLE-01 verification gaps by migrating final layout/sidebar/auth legacy selector dependencies to Tailwind v4 token-driven classes and tightening bridge CSS to explicit compatibility-only scope.

Purpose: Complete the production refactor by removing active app-surface reliance on `legacy-bridge.css` while keeping only irreducible platform/pseudo compatibility behavior.
Output: Tailwind-migrated `MainLayout`/sidebar/auth surfaces, narrowed bridge CSS with platform marker section, and passing lint/test/build evidence.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-VERIFICATION.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-05-SUMMARY.md
@src/components/MainLayout.tsx
@src/features/repositories/components/RepositorySidebar.tsx
@src/features/auth/components/GithubAuthPanel.tsx
@src/styles/legacy-bridge.css
@src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate MainLayout and repository sidebar shells off legacy class selectors</name>
  <files>src/components/MainLayout.tsx, src/features/repositories/components/RepositorySidebar.tsx</files>
  <action>Replace `layout`, `is-issue-panel-open`, `sidebar-left`, and repository `sidebar-*` style hooks with Tailwind v4 utility classes that use existing CSS tokens (`var(--...)`) for colors/spacing/sizing. Keep current data flow, callback wiring, and repository empty/loading/error semantics unchanged.</action>
  <verify>
    <automated>pnpm build &amp;&amp; ! rg -n 'class="(layout|sidebar-left|sidebar-repositories-panel|sidebar-repositories|sidebar-repositories-empty|sidebar-repositories-error|sidebar-repository-item|sidebar-repository-label|sidebar-repository-name|sidebar-repository-visibility)' src/components/MainLayout.tsx src/features/repositories/components/RepositorySidebar.tsx</automated>
  </verify>
  <done>MainLayout and repository sidebar render with token-driven Tailwind classes and no longer depend on legacy layout/sidebar selector names.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate auth footer styles and reduce legacy bridge to residual compatibility with explicit marker</name>
  <files>src/features/auth/components/GithubAuthPanel.tsx, src/styles/legacy-bridge.css</files>
  <action>Replace auth `sidebar-*` class hooks in `GithubAuthPanel.tsx` with Tailwind utilities using existing tokens. In `legacy-bridge.css`, delete remaining layout/sidebar/auth selector blocks and mobile overrides tied to those blocks. Add an explicit section heading for platform compatibility selectors and keep only irreducible compatibility concerns (platform pseudo/content selectors, highlight token hooks, global drag lock helper, and keyframes).</action>
  <verify>
    <automated>rg -n "Platform-specific compatibility selectors" src/styles/legacy-bridge.css &amp;&amp; ! rg -n "\\.(layout|sidebar-left|sidebar-repositories-panel|sidebar-repositories|sidebar-footer|sidebar-auth-error|sidebar-connect-link|sidebar-device-flow|sidebar-github-user|sidebar-signout-btn)" src/styles/legacy-bridge.css &amp;&amp; ! rg -n 'class="sidebar-' src/features/auth/components/GithubAuthPanel.tsx &amp;&amp; pnpm build</automated>
  </verify>
  <done>Auth UI no longer depends on legacy selector classes and legacy-bridge.css contains only explicitly marked residual compatibility scope.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run full verification gate after migration and resolve any migration regressions</name>
  <files>src/components/MainLayout.tsx, src/features/repositories/components/RepositorySidebar.tsx, src/features/auth/components/GithubAuthPanel.tsx, src/styles/legacy-bridge.css</files>
  <action>Run the full verification suite (`lint`, `test`, `build`) after the migration. If any failures are introduced by the style/class migration, apply minimal fixes in the migrated files and re-run until all checks pass.</action>
  <verify>
    <automated>pnpm run lint &amp;&amp; pnpm test &amp;&amp; pnpm build</automated>
  </verify>
  <done>Lint/test/build all pass on the migrated codepath and STYLE-01 gap closure evidence is complete for follow-up verification.</done>
</task>

</tasks>

<verification>
- `legacy-bridge.css` retains only residual compatibility rules and has a clearly labeled platform compatibility selector section.
- `MainLayout`, `RepositorySidebar`, and `GithubAuthPanel` use token-driven Tailwind classes instead of legacy class-selector styling hooks.
- `pnpm run lint`, `pnpm test`, and `pnpm build` pass after migration.
</verification>

<success_criteria>
- Verification gap #1 is closed: legacy bridge scope is residual-only.
- Verification gap #2 and #3 are closed: remaining layout/sidebar/auth styles are Tailwind token-driven and bridge compatibility marker is explicit.
- Verification gap #4 is closed: lint/test/build pass after migration with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-06-SUMMARY.md`.
</output>
