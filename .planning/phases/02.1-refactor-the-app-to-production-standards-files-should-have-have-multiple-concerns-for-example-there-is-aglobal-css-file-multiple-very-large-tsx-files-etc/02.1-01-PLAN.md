---
phase: 02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vite.config.ts
  - src/styles/index.css
  - src/styles/tokens.css
  - src/styles/legacy-bridge.css
  - src/App.tsx
autonomous: true
requirements:
  - STYLE-01
must_haves:
  truths:
    - "Renderer styling pipeline runs through Tailwind v4 while preserving current desktop UI behavior."
    - "All new styling work is routed through shared tokens/CSS variables instead of hardcoded colors."
    - "Legacy global styling remains available through an explicit bridge so migration can be done safely by slice."
  artifacts:
    - path: package.json
      provides: "Tailwind v4 + Vite integration dependencies."
      contains: "@tailwindcss/vite"
    - path: vite.config.ts
      provides: "Tailwind v4 plugin registration in Vite."
      contains: "tailwindcss()"
    - path: src/styles/index.css
      provides: "Single style entrypoint that imports Tailwind and project style layers."
      contains: '@import "tailwindcss";'
    - path: src/styles/tokens.css
      provides: "Canonical CSS token definitions used by Tailwind/theme mappings."
      contains: "--app-bg"
    - path: src/styles/legacy-bridge.css
      provides: "Temporary compatibility layer for not-yet-migrated selectors."
      contains: ".layout"
  key_links:
    - from: src/App.tsx
      to: src/styles/index.css
      via: "top-level stylesheet import"
      pattern: "import \"./styles/index.css\""
    - from: src/styles/index.css
      to: src/styles/tokens.css
      via: "import ordering before bridge stylesheet"
      pattern: "@import \"./tokens.css\""
    - from: vite.config.ts
      to: package.json
      via: "plugin call uses installed package"
      pattern: "@tailwindcss/vite"
---

<objective>
Establish the production styling foundation for this phase by enabling Tailwind v4 and splitting current global styles into token and legacy-bridge layers.

Purpose: Unblock incremental refactors while enforcing project rules (Tailwind v4 when possible and no hardcoded colors in new work).
Output: Tailwind v4 Vite integration, new style entry architecture, and App-level import cutover.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-RESEARCH.md
@package.json
@vite.config.ts
@src/App.tsx
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and wire Tailwind v4 in the renderer build pipeline</name>
  <files>package.json, pnpm-lock.yaml, vite.config.ts</files>
  <action>Add `tailwindcss` and `@tailwindcss/vite` as dev dependencies and register `tailwindcss()` in `vite.config.ts` ahead of `solid()`. Preserve all existing Tauri dev-server options and avoid adding Tailwind v3-era PostCSS/config files.</action>
  <verify>
    <automated>pnpm install --frozen-lockfile &amp;&amp; pnpm build</automated>
  </verify>
  <done>Vite builds successfully with Tailwind v4 plugin enabled and no runtime config regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Create token-first stylesheet layering with a legacy bridge</name>
  <files>src/styles/index.css, src/styles/tokens.css, src/styles/legacy-bridge.css</files>
  <action>Create `src/styles/index.css` as the only stylesheet entrypoint that imports Tailwind, then token definitions, then legacy bridge styles. Move current token declarations into `tokens.css` and keep migrated-parity selectors in `legacy-bridge.css` so later plans can remove sections slice-by-slice without breaking UI behavior.</action>
  <verify>
    <automated>test -f src/styles/index.css &amp;&amp; test -f src/styles/tokens.css &amp;&amp; test -f src/styles/legacy-bridge.css &amp;&amp; pnpm build</automated>
  </verify>
  <done>Style architecture is split into explicit layers with tokens isolated from compatibility selectors.</done>
</task>

<task type="auto">
  <name>Task 3: Switch app shell to the new style entrypoint</name>
  <files>src/App.tsx</files>
  <action>Replace direct `App.css` import with `src/styles/index.css` in `App.tsx` so all UI styling resolves through the new Tailwind + token + bridge pipeline. Keep app rendering behavior unchanged in this plan.</action>
  <verify>
    <automated>rg -n \"styles/index\\.css\" src/App.tsx &amp;&amp; pnpm build</automated>
  </verify>
  <done>App shell consumes the new style pipeline and still compiles/renders with existing behavior.</done>
</task>

</tasks>

<verification>
- Tailwind v4 plugin is active in Vite and build output remains healthy.
- Token and legacy bridge styles are separated into dedicated files.
- App shell imports only the new style entrypoint.
</verification>

<success_criteria>
- Tailwind v4 is wired and ready for component-level utility migration.
- Style layering supports incremental migration without feature regressions.
- New styling work has a canonical token source and a controlled legacy bridge.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-01-SUMMARY.md`.
</output>
