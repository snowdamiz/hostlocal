---
phase: 02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/issue-content/issue-body.ts
  - src/features/issue-content/issue-body.test.ts
  - src/features/board/column-inference.ts
  - src/features/board/column-inference.test.ts
  - src/components/MainLayout.tsx
autonomous: true
requirements:
  - STYLE-01
must_haves:
  truths:
    - "Issue body markdown/code content continues to render correctly, including safe fallback escaping for unsupported highlighting paths."
    - "Items open in the expected default board columns for closed, pull request, in-progress, and todo conditions."
    - "Issue rendering and default-column behavior remain stable after logic extraction from MainLayout."
  artifacts:
    - path: src/features/issue-content/issue-body.ts
      provides: "Pure issue-body parsing and highlighting helpers."
      exports:
        - parseIssueBody
        - parseIssueInlineTokens
        - highlightIssueCode
    - path: src/features/issue-content/issue-body.test.ts
      provides: "Regression tests for markdown/code parsing and escaping behavior."
      contains: "describe(\"parseIssueBody\")"
    - path: src/features/board/column-inference.ts
      provides: "Single source of truth for default board column derivation."
      exports:
        - inferDefaultColumn
    - path: src/features/board/column-inference.test.ts
      provides: "Column inference behavior tests for open/closed/PR/label cases."
      contains: "describe(\"inferDefaultColumn\")"
  key_links:
    - from: src/components/MainLayout.tsx
      to: src/features/issue-content/issue-body.ts
      via: "imports replace inline parser/highlighter helpers"
      pattern: "from \"../features/issue-content/issue-body\""
    - from: src/components/MainLayout.tsx
      to: src/features/board/column-inference.ts
      via: "imports replace inline inferDefaultColumn logic"
      pattern: "from \"../features/board/column-inference\""
    - from: src/features/board/column-inference.ts
      to: src/features/board/column-inference.test.ts
      via: "unit assertions capture label/assignee semantics"
      pattern: "it("
---

<objective>
Extract pure logic from `MainLayout.tsx` into feature modules with test coverage so later UI decomposition can proceed without behavior drift.

Purpose: Reduce monolithic coupling and establish stable contracts for parsing/highlighting and board state derivation.
Output: Feature-level parser/inference modules with tests and import-based usage from `MainLayout`.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-RESEARCH.md
@src/components/MainLayout.tsx
@src/lib/commands.ts

<interfaces>
From `src/lib/commands.ts`:
```typescript
export interface GithubRepositoryItem {
  id: number;
  number: number;
  state: "open" | "closed";
  isPullRequest: boolean;
  labels: string[];
  assignees: string[];
  body: string | null;
  updatedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract issue-body parsing/highlighting helpers into a feature module</name>
  <files>src/features/issue-content/issue-body.ts, src/features/issue-content/issue-body.test.ts</files>
  <behavior>
    - Code-fence parsing preserves language labels and block boundaries.
    - Inline backtick parsing preserves plain text around inline code segments.
    - Highlight fallback path safely escapes HTML when language parsing fails.
  </behavior>
  <action>Create `issue-body.ts` and move `escapeHtml`, block parsing, inline token parsing, and highlight logic out of `MainLayout`. Add focused Vitest coverage for fence parsing, unmatched backticks, and HTML escape fallback behavior.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/issue-content/issue-body.test.ts</automated>
  </verify>
  <done>Issue-body logic is exported from a dedicated module with tests covering core parsing/highlighting edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract default board-column inference into a tested board-domain module</name>
  <files>src/features/board/column-inference.ts, src/features/board/column-inference.test.ts</files>
  <behavior>
    - Closed items map to `done`.
    - Pull requests map to `inReview`.
    - Open issues with assignees or in-progress labels map to `inProgress`.
    - Remaining open issues map to `todo`.
  </behavior>
  <action>Create `column-inference.ts` containing `inferDefaultColumn` and related label-prefix constants. Add table-driven tests for label casing, assignee fallback, pull-request precedence, and closed-state precedence.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/column-inference.test.ts</automated>
  </verify>
  <done>Column inference behavior is centralized and regression-tested outside `MainLayout`.</done>
</task>

<task type="auto">
  <name>Task 3: Replace inline helper implementations in MainLayout with module imports</name>
  <files>src/components/MainLayout.tsx</files>
  <action>Update `MainLayout.tsx` to import extracted helpers from `src/features/issue-content/issue-body.ts` and `src/features/board/column-inference.ts`, then remove duplicated inline implementations. Keep existing runtime behavior and existing issue-panel rendering output unchanged.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/issue-content/issue-body.test.ts src/features/board/column-inference.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>MainLayout delegates parsing and column inference to imported modules and continues to compile and render.</done>
</task>

</tasks>

<verification>
- New logic modules are covered by focused automated tests.
- MainLayout no longer contains duplicated parser/inference implementations.
- Build passes with extracted-module imports.
</verification>

<success_criteria>
- Critical board/issue logic is isolated from UI rendering concerns.
- Behavior is protected with deterministic tests before broader decomposition begins.
- MainLayout line count starts decreasing without feature regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-02-SUMMARY.md`.
</output>
