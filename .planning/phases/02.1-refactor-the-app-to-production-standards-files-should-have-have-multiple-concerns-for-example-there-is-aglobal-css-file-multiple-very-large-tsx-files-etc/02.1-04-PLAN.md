---
phase: 02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc
plan: 04
type: execute
wave: 3
depends_on:
  - 02.1-03
files_modified:
  - src/features/board/types.ts
  - src/features/board/hooks/useBoardCanvas.ts
  - src/features/board/hooks/useBoardInteractions.ts
  - src/features/board/components/KanbanBoard.tsx
  - src/features/board/components/IssueDetailsPanel.tsx
  - src/components/MainLayout.tsx
autonomous: true
requirements:
  - STYLE-01
must_haves:
  truths:
    - "Users can pan, zoom, and reset the board canvas and drag cards without interaction regressions."
    - "Todo/InProgress intake transitions keep Phase 2 trigger/revert semantics and rejection feedback behavior."
    - "Selecting a board card still opens issue details with working issue-link actions."
  artifacts:
    - path: src/features/board/types.ts
      provides: "Shared board contracts used by hooks and components."
      exports:
        - KanbanColumnKey
        - DragGhostState
    - path: src/features/board/hooks/useBoardCanvas.ts
      provides: "Canvas view state and grid rendering behavior."
      exports:
        - useBoardCanvas
    - path: src/features/board/hooks/useBoardInteractions.ts
      provides: "Drag/drop state machine and intake trigger integration."
      exports:
        - useBoardInteractions
    - path: src/features/board/components/KanbanBoard.tsx
      provides: "Board columns/cards rendering layer."
      contains: "role=\"list\""
    - path: src/features/board/components/IssueDetailsPanel.tsx
      provides: "Selected issue details rendering layer."
      contains: "Open on GitHub"
  key_links:
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/lib/commands.ts
      via: "githubAttemptIssueIntake/githubRevertIssueIntake command calls"
      pattern: "githubAttemptIssueIntake"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/intake/toast-store.ts
      via: "policy rejection surfacing"
      pattern: "pushIntakeRejectionToast"
    - from: src/components/MainLayout.tsx
      to: src/features/board/components/KanbanBoard.tsx
      via: "main board rendering composition"
      pattern: "<KanbanBoard"
---

<objective>
Split board/canvas/issue-panel concerns from `MainLayout.tsx` into feature-level hooks and presentational components.

Purpose: Complete the primary architectural decomposition of the largest file without changing Phase 2 intake behavior.
Output: Board contracts, board hooks, board components, and a significantly reduced MainLayout orchestration shell.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-RESEARCH.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-02-SUMMARY.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-03-SUMMARY.md
@src/components/MainLayout.tsx
@src/lib/commands.ts
@src/intake/intake-state.ts
@src/intake/toast-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared board contracts before extraction</name>
  <files>src/features/board/types.ts</files>
  <action>Create `src/features/board/types.ts` with all board-domain contracts used across extracted hooks/components (column keys, drag ghost state, drag context, canvas view model, and any shared callback signatures). Use this as the stable interface boundary for the next tasks.</action>
  <verify>
    <automated>test -f src/features/board/types.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>Board interface contracts exist and are imported by downstream extraction tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Extract canvas and drag/intake orchestration into board hooks</name>
  <files>src/features/board/hooks/useBoardCanvas.ts, src/features/board/hooks/useBoardInteractions.ts, src/components/MainLayout.tsx</files>
  <action>Move canvas grid rendering, pan/zoom/reset behavior, pointer lifecycle management, drag-ghost animation, and intake-triggered drop orchestration into `useBoardCanvas` and `useBoardInteractions`. Preserve exact trigger semantics (`todo -> inProgress` and `inProgress -> todo` only), duplicate-attempt guards, and rejection toast behavior.</action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run src/intake/policy-reasons.test.ts src/intake/toast-store.test.ts</automated>
  </verify>
  <done>Board interaction logic is feature-owned hooks with behavior parity for canvas and intake flow.</done>
</task>

<task type="auto">
  <name>Task 3: Extract board and issue-detail rendering into presentational components</name>
  <files>src/features/board/components/KanbanBoard.tsx, src/features/board/components/IssueDetailsPanel.tsx, src/components/MainLayout.tsx</files>
  <action>Create `KanbanBoard` and `IssueDetailsPanel` components that receive typed props from `MainLayout`/board hooks. Move JSX-heavy rendering blocks out of `MainLayout` while preserving accessibility roles, card selection behavior, drag affordances, and issue-link action behavior.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>MainLayout composes extracted board components and no longer contains full board/issue-panel JSX internals.</done>
</task>

</tasks>

<verification>
- Board interactions and intake/revert behavior still execute with the same semantics.
- Canvas interactions (pan/zoom/reset) remain functional.
- MainLayout composition is simplified and board internals are extracted.
</verification>

<success_criteria>
- The monolithic MainLayout concern set is split into board-specific hooks/components.
- Phase 2 policy-gated intake behavior remains intact after refactor.
- Board module boundaries are explicit and maintainable.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-04-SUMMARY.md`.
</output>
