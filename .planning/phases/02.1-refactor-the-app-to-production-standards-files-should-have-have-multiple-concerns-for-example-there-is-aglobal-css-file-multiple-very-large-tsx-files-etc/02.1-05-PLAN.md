---
phase: 02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc
plan: 05
type: execute
wave: 4
depends_on:
  - 02.1-01
  - 02.1-04
files_modified:
  - src/components/SetupWizard.tsx
  - src/components/WindowControls.tsx
  - src/components/IntakeToastViewport.tsx
  - src/features/board/components/KanbanBoard.tsx
  - src/features/board/components/IssueDetailsPanel.tsx
  - src/styles/tokens.css
  - src/styles/legacy-bridge.css
  - eslint.config.js
  - package.json
autonomous: true
requirements:
  - STYLE-01
must_haves:
  truths:
    - "Primary refactor-targeted UI surfaces are styled with Tailwind v4 utilities backed by project design tokens."
    - "Legacy bridge CSS is reduced to irreducible platform/pseudo-element/animation cases only."
    - "Automated quality gates prevent recurrence of oversized files and hardcoded color regressions."
  artifacts:
    - path: src/styles/legacy-bridge.css
      provides: "Residual compatibility styles only for non-Tailwind edge cases."
      contains: "platform-specific selectors"
    - path: eslint.config.js
      provides: "Complexity and file-size guardrails for production standards."
      contains: "max-lines"
    - path: package.json
      provides: "Runnable lint/build/test scripts used as refactor guardrails."
      contains: "lint"
    - path: src/features/board/components/KanbanBoard.tsx
      provides: "Tailwind utility-driven board layout with tokenized colors."
      contains: "class="
    - path: src/components/SetupWizard.tsx
      provides: "Tailwind utility-driven setup flow layout with tokenized styles."
      contains: "class="
  key_links:
    - from: src/features/board/components/KanbanBoard.tsx
      to: src/styles/tokens.css
      via: "Tailwind classes consume tokenized theme variables"
      pattern: "var(--"
    - from: eslint.config.js
      to: package.json
      via: "lint scripts execute configured rules"
      pattern: "eslint"
    - from: src/styles/index.css
      to: src/styles/legacy-bridge.css
      via: "only residual bridge import after utility migration"
      pattern: "@import \"./legacy-bridge.css\""
---

<objective>
Complete the production-standard refactor by migrating major UI surfaces to Tailwind v4 utilities, shrinking legacy CSS, and adding automated anti-regression guardrails.

Purpose: Enforce durable architecture/styling standards so the codebase does not regress into monolithic files and global hardcoded-style drift.
Output: Tailwind-based component styling for key surfaces, reduced bridge CSS, and lint rules/scripts for complexity and file-size limits.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-RESEARCH.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-01-SUMMARY.md
@.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-04-SUMMARY.md
@src/styles/tokens.css
@src/styles/legacy-bridge.css
@src/components/SetupWizard.tsx
@src/components/WindowControls.tsx
@src/components/IntakeToastViewport.tsx
@src/features/board/components/KanbanBoard.tsx
@src/features/board/components/IssueDetailsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate setup, window controls, and toast surfaces to Tailwind v4 utilities</name>
  <files>src/components/SetupWizard.tsx, src/components/WindowControls.tsx, src/components/IntakeToastViewport.tsx, src/styles/legacy-bridge.css</files>
  <action>Replace class-driven styling for setup/window-control/toast surfaces with Tailwind utilities using token-backed values (`var(--token)` mappings from `tokens.css`). Remove migrated selector blocks from `legacy-bridge.css`. Do not introduce any hardcoded hex/rgb color literals in TSX or new CSS.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Core non-board UI surfaces for setup/window controls/toasts render from Tailwind classes and corresponding legacy CSS sections are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate board and issue-detail components to tokenized Tailwind classes and slim bridge CSS</name>
  <files>src/features/board/components/KanbanBoard.tsx, src/features/board/components/IssueDetailsPanel.tsx, src/styles/tokens.css, src/styles/legacy-bridge.css</files>
  <action>Apply Tailwind v4 utility classes to board columns/cards/panels/metadata and issue-detail surfaces created in Plan 04. Keep only irreducible CSS in bridge files (platform pseudo-elements, keyframes, highlight.js theming hooks, and global drag-state helpers). Ensure color/spacing/typography references resolve through tokens, not literals.</action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run</automated>
  </verify>
  <done>Board-related visual styling is utility-first and legacy bridge CSS is minimized to justified edge cases.</done>
</task>

<task type="auto">
  <name>Task 3: Add production guardrails for file size, complexity, and hardcoded-color regressions</name>
  <files>eslint.config.js, package.json</files>
  <action>Extend the existing ESLint setup with `max-lines` and `complexity` rules tuned for this codebase, expose lint scripts in `package.json`, and include a scriptable hardcoded-color check that fails if color literals appear outside `src/styles/tokens.css`. Keep thresholds strict enough to block future mega-files while allowing current extracted architecture, and avoid adding new dependencies.</action>
  <verify>
    <automated>pnpm exec eslint src --max-warnings=0 &amp;&amp; bash -lc 'if rg -n \"#([0-9a-fA-F]{3,8})\" src --glob \"*.ts\" --glob \"*.tsx\" --glob \"*.css\" --glob \"!src/styles/tokens.css\"; then echo \"Hardcoded color literal detected\"; exit 1; else echo \"Hardcoded color check passed\"; fi'</automated>
  </verify>
  <done>Automated checks enforce production standards and prevent reintroduction of giant files and hardcoded colors.</done>
</task>

</tasks>

<verification>
- Tailwind utility classes drive the major UI surfaces while token usage is preserved.
- Legacy bridge CSS is materially smaller and limited to explicit edge-case styling.
- Lint/build/test/check scripts run successfully and enforce anti-regression constraints.
</verification>

<success_criteria>
- The phase exits with production-ready module boundaries and utility-first styling foundations.
- Hardcoded colors are blocked outside token definitions.
- Complexity and line-count guardrails reduce risk of another monolithic component/styles file.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-refactor-the-app-to-production-standards-files-should-have-have-multiple-concerns-for-example-there-is-aglobal-css-file-multiple-very-large-tsx-files-etc/02.1-05-SUMMARY.md`.
</output>
