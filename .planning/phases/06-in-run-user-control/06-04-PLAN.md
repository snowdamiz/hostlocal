---
phase: 06-in-run-user-control
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - src/components/MainLayout.tsx
  - src/features/board/components/IssueDetailsPanel.tsx
  - src/features/board/components/KanbanBoard.tsx
  - src/features/board/column-inference.ts
  - src/features/board/column-inference.test.ts
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "User sees pause/resume/abort controls inline near current runtime stage and controls are enabled only for valid run states."
    - "Abort action requires explicit confirmation before execution."
    - "User can submit steering text from IssueDetailsPanel one instruction at a time with pending disable."
    - "Paused runs display an explicit paused indicator in both board cards and issue details while remaining in In Progress."
  artifacts:
    - path: src/features/board/components/IssueDetailsPanel.tsx
      provides: "Inline control row, steering composer, and abort confirmation modal."
      contains: "Current Runtime Stage"
    - path: src/features/board/components/KanbanBoard.tsx
      provides: "Card-level paused indicator alongside runtime stage metadata."
      contains: "runtime().stage"
    - path: src/components/MainLayout.tsx
      provides: "Control callback and state prop wiring from board hook to IssueDetailsPanel."
      contains: "<IssueDetailsPanel"
  key_links:
    - from: src/components/MainLayout.tsx
      to: src/features/board/hooks/useBoardInteractions.ts
      via: "Main layout passes control callbacks and pending/eligibility accessors to issue panel."
      pattern: "useBoardInteractions"
    - from: src/features/board/components/IssueDetailsPanel.tsx
      to: src/features/board/hooks/useBoardInteractions.ts
      via: "Pause/resume/abort/steer controls call hook-provided handlers scoped to selected issue."
      pattern: "onPauseRun"
    - from: src/features/board/components/KanbanBoard.tsx
      to: src/lib/commands.ts
      via: "Card runtime badges consume paused metadata from snapshot payload fields."
      pattern: "RuntimeRepositoryRunSnapshotItem"
---

<objective>
Deliver the selected-issue runtime control UI for pause, resume, abort, and steering.

Purpose: Fulfill user-facing Phase 6 behavior in the existing board/details surfaces without adding global or multi-issue control scope.
Output: IssueDetailsPanel control row + steering composer + abort modal, paused-state badges, and main layout prop wiring.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-in-run-user-control/06-CONTEXT.md
@.planning/phases/06-in-run-user-control/06-RESEARCH.md
@src/components/MainLayout.tsx
@src/features/board/components/IssueDetailsPanel.tsx
@src/features/board/components/KanbanBoard.tsx
@src/features/board/column-inference.ts

<interfaces>
From `src/features/board/components/IssueDetailsPanel.tsx`:
```ts
interface IssueDetailsPanelProps {
  selectedBoardItem: Accessor<GithubRepositoryItem | null>;
  selectedBoardRuntime: Accessor<RuntimeRepositoryRunSnapshotItem | null>;
  selectedBoardRuntimeHistory: Accessor<RuntimeIssueRunHistoryItem[]>;
  selectedBoardRuntimeTelemetry: Accessor<RuntimeRunTelemetryEventPayload[]>;
  selectedBoardRuntimeSummary: Accessor<RuntimeIssueRunSummary | null>;
}
```

From `src/components/MainLayout.tsx`:
```tsx
<IssueDetailsPanel
  selectedBoardItem={selectedBoardItem}
  selectedBoardRuntime={selectedBoardRuntime}
  selectedBoardRuntimeHistory={selectedBoardRuntimeHistory}
  selectedBoardRuntimeTelemetry={selectedBoardRuntimeTelemetry}
  selectedBoardRuntimeSummary={selectedBoardRuntimeSummary}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire control props through MainLayout and establish panel control contract</name>
  <files>src/components/MainLayout.tsx, src/features/board/components/IssueDetailsPanel.tsx</files>
  <action>Extend `IssueDetailsPanelProps` with control callbacks, action eligibility flags, pending state, and steering input handlers returned by `useBoardInteractions`. Update `MainLayout` destructuring and prop passing so IssueDetailsPanel receives selected-issue scoped control APIs without adding new global control surfaces.</action>
  <verify>
    <automated>pnpm run build</automated>
  </verify>
  <done>IssueDetailsPanel has complete typed control contract and MainLayout compiles with new control prop wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Implement IssueDetailsPanel control row, steering composer, and abort confirmation modal</name>
  <files>src/features/board/components/IssueDetailsPanel.tsx</files>
  <action>Render an inline action row near the `Current Runtime Stage` section with Pause/Resume/Abort controls and state-aware disabled/label behavior based on hook-provided eligibility + pending flags. Add a freeform steering composer below the action row with Enter/button send, single-flight pending disable, and helper copy. Add an explicit confirmation modal for abort (blocking action trigger until confirmed). Keep styling Tailwind v4 utility-first and token-based (`var(--...)`) only.</action>
  <verify>
    <automated>pnpm run build</automated>
  </verify>
  <done>Users can operate pause/resume/abort/steer controls from IssueDetailsPanel with required guardrails and explicit abort confirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render paused indicators in board/panel runtime badges and keep column inference deterministic</name>
  <files>src/features/board/components/KanbanBoard.tsx, src/features/board/components/IssueDetailsPanel.tsx, src/features/board/column-inference.ts, src/features/board/column-inference.test.ts</files>
  <behavior>
    - Paused runs show an explicit paused badge in card and panel runtime sections.
    - Paused active runs remain grouped in In Progress (no queue-removal remap).
    - Existing terminal precedence rules remain unchanged.
  </behavior>
  <action>Update runtime badge rendering in KanbanBoard and IssueDetailsPanel to include paused visual state from runtime metadata. Keep column inference deterministic by preserving existing terminal/non-terminal precedence and ensuring paused active runs do not drift to Todo/Review. Expand `column-inference.test.ts` with paused metadata scenarios that protect In Progress mapping guarantees.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/column-inference.test.ts &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Paused state is visible across board/panel while board column behavior remains deterministic and regression-tested.</done>
</task>

</tasks>

<verification>
- IssueDetailsPanel shows state-aware controls only when runtime metadata exists for the selected issue.
- Abort cannot execute without explicit modal confirmation.
- Steering submit path enforces one in-flight instruction at a time.
- Paused runs remain in In Progress with explicit paused badges in both card and panel contexts.
</verification>

<success_criteria>
- UI delivers all locked Phase 6 control-entry decisions without introducing deferred global/multi-issue features.
- Pause/resume/abort/steer interactions are discoverable and gated correctly in the selected issue panel.
- Board and panel runtime badges reflect paused state consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/06-in-run-user-control/06-04-SUMMARY.md`
</output>
