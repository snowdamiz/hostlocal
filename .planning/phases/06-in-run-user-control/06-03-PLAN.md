---
phase: 06-in-run-user-control
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/lib/commands.ts
  - src/features/board/hooks/useBoardInteractions.ts
  - src/features/board/hooks/useBoardInteractions.test.ts
  - src/intake/policy-reasons.ts
  - src/intake/policy-reasons.test.ts
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "Frontend can invoke pause/resume/abort/steer commands for the selected issue using typed request/outcome contracts."
    - "Hook exposes state-aware control eligibility and pending state for selected runtime metadata."
    - "Each control action acknowledgement triggers telemetry-compatible state refresh plus visible toast feedback."
  artifacts:
    - path: src/lib/commands.ts
      provides: "Typed runtime control command wrappers and paused-state payload fields."
      contains: "runtimePauseIssueRun"
    - path: src/features/board/hooks/useBoardInteractions.ts
      provides: "Control action orchestration, selected-run eligibility helpers, and control toast emission."
      contains: "selectedBoardRuntime"
    - path: src/intake/policy-reasons.ts
      provides: "Reason/fix copy for control-specific reason codes shown in panel metadata."
      contains: "runtime_"
  key_links:
    - from: src/lib/commands.ts
      to: src-tauri/src/lib.rs
      via: "Wrapper command names map exactly to Tauri invoke handlers added in backend plan."
      pattern: "runtime_pause_issue_run"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/runtime-control/toast-store.ts
      via: "Control outcomes emit acknowledgement toasts through runtime control store APIs."
      pattern: "pushRuntimeControlToast"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/features/board/components/IssueDetailsPanel.tsx
      via: "Hook returns callbacks + pending/eligibility flags consumed by control row and steering composer."
      pattern: "onPauseRun"
---

<objective>
Wire frontend runtime control contracts and board hook orchestration for pause, resume, abort, and steering.

Purpose: Bridge backend control commands to selected-issue board state with deterministic gating, acknowledgements, and test coverage.
Output: Updated command typings/wrappers, hook control handlers/state, reason copy updates, and expanded unit tests.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-in-run-user-control/06-CONTEXT.md
@.planning/phases/06-in-run-user-control/06-RESEARCH.md
@src/lib/commands.ts
@src/features/board/hooks/useBoardInteractions.ts
@src/features/board/hooks/useBoardInteractions.test.ts
@src/intake/policy-reasons.ts
@src/runtime-control/toast-store.ts

<interfaces>
From `src/lib/commands.ts`:
```ts
export interface RuntimeRunStageChangedEventPayload {
  runId: number;
  repositoryFullName: string;
  repositoryKey: string;
  issueNumber: number;
  issueTitle: string;
  issueBranchName: string;
  stage: RuntimeRunStage;
  queuePosition: number | null;
  terminalStatus: RuntimeTerminalStatus | null;
  reasonCode: string | null;
  fixHint: string | null;
}
```

From `src/features/board/hooks/useBoardInteractions.ts`:
```ts
const selectedBoardRuntime = createMemo(() => {
  const item = selectedBoardItem();
  if (!item) return null;
  return runtimeSnapshotByIssueNumber()[item.number] ?? null;
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend command contracts for control commands and paused-state payload fields</name>
  <files>src/lib/commands.ts</files>
  <behavior>
    - Frontend has typed invoke wrappers for pause/resume/abort/steer command names.
    - Snapshot/history/stage payload contracts include paused metadata from backend.
    - Control request/outcome types carry run targeting, acknowledgement, and reason/fix metadata.
  </behavior>
  <action>Add typed request/outcome interfaces and invoke wrappers for `runtime_pause_issue_run`, `runtime_resume_issue_run`, `runtime_abort_issue_run`, and `runtime_steer_issue_run`. Extend runtime snapshot/history/stage payload interfaces with paused-state fields so board UI can render paused indicators and gate action availability without `any` casts.</action>
  <verify>
    <automated>pnpm run build</automated>
  </verify>
  <done>Frontend compile-time contracts match backend control APIs and paused metadata requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add selected-issue control orchestration and pending state to board hook</name>
  <files>src/features/board/hooks/useBoardInteractions.ts, src/features/board/hooks/useBoardInteractions.test.ts</files>
  <behavior>
    - Hook exposes control action callbacks plus state-aware eligibility (pause/resume/abort/steer).
    - Steering submissions are serialized one-at-a-time with pending disable semantics.
    - Control outcomes trigger snapshot/history/telemetry refresh and runtime-control toast acknowledgements.
  </behavior>
  <action>Implement hook-level control helpers that operate only on selected issue + repository scope, enforce locked decision eligibility rules (pause/resume active-only, abort active/paused-only, steering active-only), and publish acknowledgement toasts through runtime-control store. Include optimistic pending state + post-action hydration and keep queued-run behavior unchanged.</action>
  <verify>
    <automated>pnpm exec vitest run src/features/board/hooks/useBoardInteractions.test.ts</automated>
  </verify>
  <done>Board hook can drive all runtime controls deterministically with test coverage for eligibility, pending guards, and acknowledgement behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand reason-code copy mapping for control outcomes and keep tests synchronized</name>
  <files>src/intake/policy-reasons.ts, src/intake/policy-reasons.test.ts, src/features/board/hooks/useBoardInteractions.test.ts</files>
  <behavior>
    - Control-specific reason codes resolve to user-facing violated-rule/fix-hint copy.
    - Abort cancellation metadata displays meaningful reason/fix text in existing runtime panel reason surface.
    - Hook tests verify rejection paths preserve reasonCode/fixHint semantics for toasts and panel consumption.
  </behavior>
  <action>Add control reason codes used by backend and hook paths to `INTAKE_POLICY_REASON_MAP` with clear copy for invalid-state, pause/resume, abort, and steering acknowledgement failures. Update tests to assert mapping completeness and hook-level behavior against new control outcome contracts.</action>
  <verify>
    <automated>pnpm exec vitest run src/intake/policy-reasons.test.ts src/features/board/hooks/useBoardInteractions.test.ts &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Frontend control orchestration and reason messaging are stable, typed, and regression-tested.</done>
</task>

</tasks>

<verification>
- New command wrappers compile and align with backend command registration names.
- Hook tests cover state-aware control gating plus pending/refresh behavior.
- Reason-code map includes control-related outcomes used by control flows and terminal metadata surfaces.
</verification>

<success_criteria>
- Frontend has complete typed control API coverage for CTRL-01..04.
- Selected issue control actions are gated correctly and produce deterministic acknowledgement behavior.
- Control outcome copy remains consistent with existing reasonCode/fixHint rendering surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/06-in-run-user-control/06-03-SUMMARY.md`
</output>
