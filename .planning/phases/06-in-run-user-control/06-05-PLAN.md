---
phase: 06-in-run-user-control
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/runtime_boundary.rs
autonomous: true
gap_closure: true
requirements:
  - CTRL-03
must_haves:
  truths:
    - "User aborting an active or paused run always ends in terminal `cancelled` state and still triggers workspace cleanup."
    - "Abort cleanup remains deterministic even when abort finalization wins the race before sidecar termination provides workspace context."
    - "Abort acknowledgements preserve reasonCode/fixHint semantics without introducing duplicate terminal finalization."
  artifacts:
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Active run control state stores workspace-root context and abort finalization reuses it for cleanup."
      contains: "RuntimeActiveRunControl"
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Regression tests prove workspace cleanup still occurs for abort race ordering."
      contains: "runtime_boundary_abort"
  key_links:
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "spawn-sidecar control registration persists workspace root that abort finalization reuses."
      pattern: "register_active_run_control"
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "runtime_abort_issue_run resolves workspace root from control registry before finalize_run_with_control_gate."
      pattern: "runtime_abort_issue_run"
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/src/runtime_boundary.rs
      via: "finalize_run performs cleanup through finalize_workspace_cleanup using resolved workspace path."
      pattern: "finalize_workspace_cleanup"
---

<objective>
Close the remaining Phase 06 verification gap for CTRL-03 by making abort cleanup path deterministic.

Purpose: Ensure user abort always performs safe workspace cleanup, including race orderings where abort finalizes before sidecar termination reports workspace context.
Output: Runtime control state/finalization wiring updates plus regression coverage for abort cleanup race behavior.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-in-run-user-control/06-VERIFICATION.md
@.planning/phases/06-in-run-user-control/06-01-SUMMARY.md
@src-tauri/src/runtime_boundary.rs

<interfaces>
From `src-tauri/src/runtime_boundary.rs`:
```rust
struct RuntimeActiveRunControl {
    child: Option<CommandChild>,
    is_paused: bool,
    pending_terminal: Option<RuntimeTerminalRequest>,
    abort_requested: bool,
    finalized: bool,
}

fn finalize_run_with_control_gate(
    app: &AppHandle,
    run: RuntimeIssueRun,
    workspace_root: Option<PathBuf>,
    terminal_status: RuntimeTerminalStatus,
    reason_code: Option<String>,
    fix_hint: Option<String>,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist workspace-root context in active control state and reuse it for abort finalization</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Active control registry retains workspace-root context when sidecar starts.
    - Abort finalization can resolve workspace path from control state even when child termination payload is not yet available.
    - Existing queued-run rejection, pause/resume behavior, and abort acknowledgement reason copy remain unchanged.
  </behavior>
  <action>Extend `RuntimeActiveRunControl` and control-registration helpers to capture workspace-root path at run start. Add a helper for abort paths to read workspace-root context from active control state before the control entry is finalized. Update `runtime_abort_issue_run` to pass that resolved workspace root into `finalize_run_with_control_gate` (instead of `None`) while preserving current reasonCode/fixHint and race-suppression semantics.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Abort path always has a deterministic workspace-root candidate for cleanup, regardless of abort-vs-termination ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for abort cleanup race ordering</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Tests prove workspace deletion still occurs when abort finalization executes before sidecar termination thread contributes workspace context.
    - Tests protect against duplicate-finalization regressions while validating cancelled terminal outcome behavior.
  </behavior>
  <action>Add/expand runtime-boundary tests to simulate an active run with registered control state and a real temporary workspace path, then trigger abort-path finalization ordering where workspace context must come from control state. Assert that cleanup removed the workspace directory and that terminal arbitration still suppresses duplicate finalization attempts.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture</automated>
  </verify>
  <done>Automated tests fail before the fix and pass after it, proving the CTRL-03 cleanup race gap is closed.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test runtime_boundary -- --nocapture`
- `cd src-tauri && cargo check`
- Confirm abort finalization paths no longer call `finalize_run_with_control_gate(..., None, ...)` for active/paused runs.
</verification>

<success_criteria>
- Gap from `06-VERIFICATION.md` is closed: abort path always cleans workspace deterministically.
- CTRL-03 remains satisfied end-to-end with safe cleanup semantics under race conditions.
- No regressions introduced in pause/resume/steer behavior or queue semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/06-in-run-user-control/06-05-SUMMARY.md`
</output>
