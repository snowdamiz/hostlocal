---
phase: 06-in-run-user-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/db.rs
  - src-tauri/src/runtime_boundary.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "User can pause an active run and runtime metadata exposes paused state while the issue remains in In Progress."
    - "User can resume a paused run and runtime progression can continue from the paused point."
    - "User can abort an active or paused run and receive deterministic terminal `cancelled` metadata."
    - "User can send steering instructions to an active run and receive explicit acknowledged/error outcomes."
  artifacts:
    - path: src-tauri/src/db.rs
      provides: "Persisted pause-state columns and idempotent schema migration support."
      contains: "is_paused"
    - path: src-tauri/src/runtime_boundary.rs
      provides: "Runtime control command handlers, active run control registry, and pause-aware finalization flow."
      contains: "runtime_pause_issue_run"
    - path: src-tauri/src/lib.rs
      provides: "Tauri command registration for pause/resume/abort/steer controls."
      contains: "runtime_boundary::runtime_pause_issue_run"
  key_links:
    - from: src-tauri/src/runtime_boundary.rs
      to: src-tauri/src/db.rs
      via: "Pause/resume/abort control paths persist control state and terminal metadata in runtime_runs."
      pattern: "UPDATE runtime_runs SET is_paused"
    - from: src-tauri/src/runtime_boundary.rs
      to: src/lib/commands.ts
      via: "Stage payloads include paused state used for frontend action gating and badges."
      pattern: "RuntimeRunStageChangedEventPayload"
    - from: src-tauri/src/runtime_boundary.rs
      to: src/features/board/hooks/useBoardInteractions.ts
      via: "Control commands emit runtime telemetry acknowledgements consumed by existing listeners."
      pattern: "runtime/run-telemetry"
---

<objective>
Implement backend runtime control-plane primitives for pause, resume, abort, and steering.

Purpose: Satisfy CTRL-01..CTRL-04 at the runtime boundary with deterministic command outcomes, persisted paused metadata, and safe abort finalization.
Output: New runtime control commands, paused-state persistence, stage payload extensions, and test coverage for eligibility/race behavior.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-in-run-user-control/06-CONTEXT.md
@.planning/phases/06-in-run-user-control/06-RESEARCH.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-01-SUMMARY.md
@.planning/phases/05-live-telemetry-and-safe-summaries/05-03-SUMMARY.md
@src-tauri/src/db.rs
@src-tauri/src/runtime_boundary.rs
@src-tauri/src/lib.rs

<interfaces>
From `src-tauri/src/runtime_boundary.rs`:
```rust
pub struct RuntimeBoundarySharedState {
    queue: Mutex<RuntimeBoundaryState>,
}

pub struct RuntimeRunStageChangedEventPayload {
    pub run_id: i64,
    pub repository_full_name: String,
    pub repository_key: String,
    pub issue_number: i64,
    pub stage: String,
    pub queue_position: Option<usize>,
    pub terminal_status: Option<String>,
    pub reason_code: Option<String>,
    pub fix_hint: Option<String>,
}
```

From `src-tauri/src/lib.rs`:
```rust
runtime_boundary::runtime_enqueue_issue_run,
runtime_boundary::runtime_dequeue_issue_run,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persisted paused-state fields and stage payload support</name>
  <files>src-tauri/src/db.rs, src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - `runtime_runs` persists paused state without introducing a new lifecycle stage enum value.
    - Existing databases migrate idempotently (startup-safe) without data loss.
    - Runtime snapshot/history/stage payload models expose paused state for frontend rendering.
  </behavior>
  <action>Add `is_paused` and `paused_at` persistence support for `runtime_runs` using an idempotent migration helper (schema introspection + `ALTER TABLE` when missing). Update persisted-run readers, snapshot/history builders, and stage-change payload generation to include paused metadata while keeping current stage progression contract (`queued|preparing|coding|validating|publishing`) intact.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Backend read models can represent paused runs deterministically and existing schema bootstrap works for both fresh and pre-existing DBs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce active-run control registry and pause-aware finalization gating</name>
  <files>src-tauri/src/runtime_boundary.rs</files>
  <behavior>
    - Active runs retain process handles so abort/steer commands can target live workers.
    - Pausing blocks lifecycle progression visibility until resumed.
    - Terminal races between async process termination and user control actions are handled deterministically.
  </behavior>
  <action>Refactor runtime state to track per-active-run control data (child handle, pause flag, pending terminal metadata, abort marker). Update sidecar spawn flow to retain `CommandChild` instead of dropping it, and gate terminal finalization so paused runs do not advance visible state until resume is processed. Keep queued-run behavior unchanged and preserve existing safe cleanup/finalize pathway as canonical.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Runtime can safely coordinate process events with pause/resume/abort control state without duplicate or conflicting finalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement and register pause/resume/abort/steer runtime commands</name>
  <files>src-tauri/src/runtime_boundary.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Pause/resume allowed only for active runs (not queued/terminal), with manual resume and explicit acknowledgement.
    - Abort allowed for active or paused runs and finalizes as cancelled with reason/fix metadata.
    - Steering accepts one instruction payload and returns deterministic acknowledgement/failure outcome.
  </behavior>
  <action>Add `runtime_pause_issue_run`, `runtime_resume_issue_run`, `runtime_abort_issue_run`, and `runtime_steer_issue_run` command handlers with typed request/outcome structs and state eligibility guards. Emit telemetry acknowledgements for every outcome path, preserve queue-removal semantics for queued runs, and register new commands in `tauri::generate_handler!` in `lib.rs`.</action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test runtime_boundary -- --nocapture &amp;&amp; cargo check</automated>
  </verify>
  <done>Backend exposes deterministic control endpoints that satisfy CTRL-01..04 behavior gates and compile as part of the Tauri command surface.</done>
</task>

</tasks>

<verification>
- Paused-state schema migration works on fresh and pre-existing SQLite files.
- Stage/telemetry payloads include control acknowledgement signals without regressing queue progression.
- Abort finalization remains single-path and deterministic (no duplicate terminal writes for one run).
</verification>

<success_criteria>
- Runtime boundary supports pause, resume, abort, and steering commands with explicit outcomes.
- Paused-state metadata is durable and available to frontend hydration/event streams.
- Abort routes through canonical cleanup/finalization and leaves issue state reconcilable as cancelled.
</success_criteria>

<output>
After completion, create `.planning/phases/06-in-run-user-control/06-01-SUMMARY.md`
</output>
