---
phase: 06-in-run-user-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime-control/toast-store.ts
  - src/runtime-control/toast-store.test.ts
  - src/components/RuntimeControlToastViewport.tsx
  - src/styles/tokens.css
  - src/App.tsx
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "User receives explicit toast acknowledgement when pause/resume/abort/steer actions are accepted or rejected."
    - "Repeated identical control acknowledgements collapse into a deduplicated toast entry instead of spamming."
    - "Control acknowledgement toasts are available app-wide during active run interaction."
  artifacts:
    - path: src/runtime-control/toast-store.ts
      provides: "Runtime-control toast state store with dedupe, TTL, and subscription APIs."
      contains: "pushRuntimeControlToast"
    - path: src/components/RuntimeControlToastViewport.tsx
      provides: "Accessible viewport for runtime control toasts with dismiss and auto-prune behavior."
      contains: "RuntimeControlToastViewport"
    - path: src/styles/tokens.css
      provides: "Design tokens used by runtime control toast visuals."
      contains: "--runtime-control-toast-"
  key_links:
    - from: src/App.tsx
      to: src/components/RuntimeControlToastViewport.tsx
      via: "Global app shell mounts control toast viewport so control feedback is always visible."
      pattern: "RuntimeControlToastViewport"
    - from: src/features/board/hooks/useBoardInteractions.ts
      to: src/runtime-control/toast-store.ts
      via: "Hook control outcomes publish toast acknowledgements through shared store."
      pattern: "pushRuntimeControlToast"
    - from: src/components/RuntimeControlToastViewport.tsx
      to: src/styles/tokens.css
      via: "Viewport classes consume token variables; no hardcoded color values."
      pattern: "var(--runtime-control-toast-"
---

<objective>
Build the runtime control acknowledgement toast infrastructure used by pause/resume/abort/steer UI flows.

Purpose: Provide consistent, token-driven, app-wide acknowledgement feedback for all in-run control actions.
Output: Runtime control toast store + tests, dedicated viewport component, and app-shell mounting with tokenized styling.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-in-run-user-control/06-CONTEXT.md
@.planning/phases/06-in-run-user-control/06-RESEARCH.md
@src/intake/toast-store.ts
@src/components/IntakeToastViewport.tsx
@src/styles/tokens.css
@src/App.tsx

<interfaces>
From `src/intake/toast-store.ts`:
```ts
export interface IntakeToastStore {
  getToasts: () => IntakeRejectionToast[];
  pushRejectionToast: (reasonCode: string | null | undefined, fixHint?: string | null) => IntakeRejectionToast;
  dismissToast: (toastId: string) => void;
  clearToasts: () => void;
  pruneExpired: (atMs?: number) => void;
  subscribe: (listener: (toasts: IntakeRejectionToast[]) => void) => () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create runtime control toast store with deterministic dedupe semantics</name>
  <files>src/runtime-control/toast-store.ts, src/runtime-control/toast-store.test.ts</files>
  <behavior>
    - Control acknowledgement events produce typed toasts with reason, hint, action label, and status severity.
    - Repeated same-signature acknowledgements collapse and increment count within dedupe window.
    - Store supports subscription, dismiss, clear, and periodic prune operations.
  </behavior>
  <action>Implement a runtime-control-specific toast store (do not overload intake-rejection semantics) following the existing intake store pattern. Include defaults for dedupe and auto-dismiss windows, clone-on-read safety, deterministic IDs, and helper exports (`push`, `dismiss`, `clear`, `prune`, `subscribe`). Add focused unit tests for dedupe, expiration, and listener lifecycle behavior.</action>
  <verify>
    <automated>pnpm exec vitest run src/runtime-control/toast-store.test.ts</automated>
  </verify>
  <done>Runtime control toast store is fully test-covered and ready for hook/UI integration.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime control toast viewport and mount it in app shell</name>
  <files>src/components/RuntimeControlToastViewport.tsx, src/styles/tokens.css, src/App.tsx</files>
  <action>Create `RuntimeControlToastViewport` mirroring existing accessibility and lifecycle behavior (subscribe + prune interval + dismiss). Add required `--runtime-control-toast-*` tokens in `tokens.css` and style with Tailwind utility classes that consume those tokens. Mount the viewport in `App.tsx` alongside existing global overlays so control acknowledgements are visible regardless of selected board state.</action>
  <verify>
    <automated>pnpm exec vitest run src/runtime-control/toast-store.test.ts &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Runtime control toasts render globally with token-driven styling and compile cleanly with current app shell.</done>
</task>

</tasks>

<verification>
- Runtime control toasts can be emitted, deduped, dismissed, and auto-pruned through store APIs.
- App shell includes runtime control viewport without regressing existing intake toast behavior.
- Styling uses design tokens/CSS variables only; no hardcoded color literals.
</verification>

<success_criteria>
- A dedicated toast pipeline exists for pause/resume/abort/steer acknowledgements.
- Users can see acknowledgement feedback without opening additional navigation surfaces.
- Store and viewport are reusable by hook-level control action handlers in downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/06-in-run-user-control/06-02-SUMMARY.md`
</output>
